---
description: SoundPola 产品描述优先；开发时必须同步更新信息架构文档
alwaysApply: true
---

# 产品描述与信息架构（每次必读）

## 权威来源

- 产品定位、信息架构、页面逻辑以 `first.md` 为准。
- 视觉与组件规范以 `designstyle.md` 为准。
- 冲突时：用户描述 > `first.md` > `designstyle.md`。

## 技术栈（first.md）

目标：**Flutter / React Native** 跨平台（Android + iOS）。  
当前实现：**Flutter** 工程位于 `mobile/`。

## 一级结构（不可违背）

1. **Record** — 捕捉声音与实时可视化
2. **Drafts** — 未写入声片的暂存
3. **Collection** — 已写入并上链的收藏

核心规则：录音 → Drafts → Press → Collection → Memory。Press / Memory 不进底部导航。

## 开发同步义务

每次实现或改动功能时，必须同步更新 `first.md` 对应章节。
