文档用途：Flutter App UI/UX 设计、原型制作、路由规划与开发拆分版本：V2.0视觉基调：黑色背景，主题色 #63E0CB核心对象：声音、声音视觉、NFC 声片、NFT 数字资产、用户身份技术基线：Flutter，Android 优先，并预留 iOS 适配

1. 产品核心规则

SoundPola 将一段声音从“即时捕捉”转化为“可收藏、可触碰、可回看的声音记忆”。

录音完成后，声音先进入 Drafts，不直接进入 Collection。

只有同时完成 NFC 声片写入和 NFT 上链，声音才进入 Collection。

一张 NFC 声片只对应一个声音，写入后永久绑定，不可覆盖或替换。

录制日期、时间、地点、设备和时长由系统自动记录。

用户只编辑声音名称、分类和描述文本，其中描述为选填。

Record 与 Drafts 需登录后使用（本版启动后先完成登录，再进入主流程）。

首次执行 Press、创建 NFT、同步或恢复资产时必须登录。

NFT 必须归属于一个稳定账号；App 可在后台为普通用户创建托管钱包，不强迫用户理解助记词、Gas 或链上术语。

Drafts 以“本地优先”保存；Collection、NFT 所有权与绑定记录以账号云端数据为准。

登录是 Press 的前置条件，但登录成功后必须回到原声音与原操作节点，不让用户重新寻找内容。

2. 核心对象与归属关系

对象

未登录时

登录后

是否可修改

原始声音

本地保存

可按策略备份到云端

写入声片后不可替换

声音视觉

本地生成

与声音一同同步

成为 NFT 后保留原始版本

名称 / 分类 / 描述

本地编辑

同步至账号

可修改 App 展示信息

时间 / 地点 / 时长

系统自动记录

同步至账号

不可编辑

NFC 声片绑定

不允许创建

归属于当前账号

永久绑定，不可覆盖

NFT 数字资产

不允许创建

归属于数字资产账户

链上记录不可删除或修改

Collection

不可用或仅显示登录引导

从云端同步

只展示写入并上链成功内容

3. 一级导航

底部导航保持三个核心入口，不增加第四个“我的”：

一级导航

核心任务

主要内容

Record

捕捉声音

录音、实时声音可视化、录音控制

Drafts

管理未实体化的声音

播放、删除、编辑基础信息、发起 Press

Collection

浏览已收藏的声音

已写入声片并完成上链的声音收藏

辅助入口：

Account：通过各一级页面右上角的头像进入。

Press：从 Drafts 卡片或暂存声音详情发起。

Memory：从 Collection 中的收藏卡片进入。

这种结构让“捕捉—暂存—收藏”始终是产品主叙事，账号与 NFT 管理作为支撑层存在。

4. 登录策略

4.1 启动登录（本版）

进入 App 主流程前必须完成账号密码登录或注册。

麦克风权限申请在登录之前完成；登录页不可跳过。

退出登录后返回登录页，本地 Drafts 仍保留。

（产品远期仍可支持 Record / Drafts 本地未登录使用；本版按「先登录」落地。）

4.2 必须登录的触发点

启动进入主 Tab。

点击“Press / 写入声片”。

创建 NFT 或查询链上状态。

进入 Collection 同步资产。

恢复旧设备中的声音收藏。

分享需要验证所有权的链上收藏。

4.3 推荐登录方式

MVP 优先：

手机号或邮箱作为账号标识，配合密码登录 / 注册（本版不做验证码）。

支持「记住密码」：勾选后将账号写入本地偏好、密码写入安全存储，下次打开登录页自动回填；取消勾选则清除。

登录后由系统自动创建或关联托管数字资产账户。

后续可选：

Apple / Google 登录，可按 Android 发布地区选择。

连接外部钱包。

导出或迁移钱包。

界面默认使用“数字资产账户”“链上凭证”，不把“钱包地址、Gas、Mint”等专业词放在主流程。

4.4 登录拦截与任务恢复

从 Draft 详情点击 Press 时：

系统保存当前 draftId、写入入口和已选写入方式。

未登录则以全屏页或底部弹层进入登录。

登录成功后自动创建或关联数字资产账户。

返回原 draftId 对应的 Press 流程，并恢复到登录前步骤。

用户取消登录则返回原 Draft 详情，声音和填写内容不丢失。

登录、密码校验、网络异常均不能清空当前录音或 Draft。

5. NFT 所需个人信息边界

5.1 MVP 必要字段

信息

必要性

用途

展示位置

用户 ID

必须，系统生成

账号与资产归属

不直接展示

手机号或邮箱

至少一项

登录、找回、重要通知

账号与安全

昵称

必须，可使用默认值

App 内身份展示

Account

头像

可选

个性化展示

Account

钱包地址

必须，系统生成或关联

NFT 所有权

数字资产账户

用户协议与隐私同意记录

必须

合规与授权记录

权限与隐私

国家 / 地区

条件必需

适配 NFT、服务与合规策略

首次创建资产时按需获取

5.2 不默认收集

性别、生日、职业、公司、兴趣等非必要画像。

身份证件、真实姓名和详细住址。

银行卡或支付信息。

只有当具体链、NFT 发行方式、法币交易或所在地区法规明确要求 KYC 时，才增加“身份认证”独立流程；声音写入与收藏本身不默认要求 KYC。

5.3 个人中心的信息分层

第一层：头像、昵称、收藏数量、声片数量、账号状态。

第二层：我的数字资产、声片与设备、账号与安全、权限与隐私。

第三层：钱包地址、链、合约、Token ID、交易哈希等技术信息。

技术信息默认折叠，避免个人中心变成区块链后台。

6. 全局声音状态

状态

所在空间

用户可执行操作

录音中

Record

暂停、继续、完成、取消

录音结果

Record

试听、命名、分类、描述、重新录制、保存

已暂存

Drafts

播放、编辑、删除、写入声片

待登录

Drafts / Press

登录或返回

声片检测中

Press

取消检测

写入中

Press

查看进度，不可中断

已写入、待上链

Press / Drafts

等待或重试上链，不重新写入

上链失败

Drafts

查看原因、重试上链

已写入并上链

Collection

播放、查看 Memory、分享

永久绑定

Memory

查看声片信息与链上凭证

7. 页面清单

7.1 启动与权限

01 启动页

SoundPola 标识。

黑底声音视觉动效。

登录状态检查。

本地 Drafts 与账号数据加载状态。

02 首次引导

产品价值：捕捉声音、写入声片、收藏记忆。

麦克风用途说明。

定位用途说明。

“开始使用”。

“已有账号，登录”次要入口。

03 权限申请

麦克风权限：录音必需，拒绝后禁用录音。

定位权限：用于自动记录地点，拒绝后仍可录音。

NFC 权限或系统状态：在 Press 前按需请求。

通知权限：用于上链完成或失败提醒，按需请求。

隐私政策入口。

7.2 Record

04 录音首页

顶部：SoundPola 标识、账户入口（图标 +「账户」文案，避免单字歧义）。

中央：黑底有机线束可视化（单一 Seed → 六重旋转+镜像；2–3 组主线束，中心由曲线避让形成负空间，无遮罩黑圆）。

状态文案：“点击开始捕捉声音”（提高对比度）。

麦克风与地点记录状态。

主录音按钮（待机态）靠底部导航上方放置；与可视化通过淡色轴线视觉关联。

底部三项导航（Record / Drafts / Collection，中英一致）。

05 录音中

关闭 / 取消入口。

实时声音可视化：音频分析在 Isolate（低频），Flutter 仅消费 RMS / Pitch / 低中高能量 / Onset / 置信度；主体控制点 20–30Hz 更新目标，60FPS 插值绘制；单 Seed 几何经 Canvas 六重旋转复制。

录音计时。

录音中 / 已暂停状态。

暂停 / 继续。

完成录音。

音量过低、过高或正常提示。

06 录音结果

自动生成的声音视觉主图。

播放 / 暂停、进度和时长。

自动信息：日期、时间、地点。

名称：必填，建议 20 字以内。

分类：必选，单选。

描述：选填，建议 200 字以内。

“重新录制”。

“保存至 Drafts”。

7.3 Drafts

07 Drafts 列表

顶部：标题、数量、账号头像。

搜索、筛选、排序。

状态筛选：全部、处理中、失败。

暂存卡片：

声音视觉缩略图。

声音名称。

播放按钮。

分类。

录制日期、地点、时长。

当前处理状态。

“Press”主操作。

更多菜单：编辑信息、删除、查看录制信息。

未登录提示卡：说明登录后可写入声片与同步收藏。

空状态：引导前往 Record。

08 Draft 详情

声音视觉主图。

播放控制与进度。

名称、分类、描述。

只读信息：日期、时间、地点、时长、设备。

当前状态与失败原因。

主操作：“写入声片”。

次要操作：编辑信息、删除。

已经写入 NFC 但上链失败的声音不可删除，只允许重试上链或联系客服。

7.4 登录与账户建立

09 登录页 / 登录触发页

启动进入或从 Press 进入时显示；Press 场景不中断用户对当前声音的确认。

品牌名 SoundPola（大号标题）。

从 Press 进入时：当前待写入声音的缩略图与名称。

说明：“登录后，这段声音将归属于你的数字收藏。”（启动登录用“登录后开始捕捉声音…”）

手机号 / 邮箱 + 密码（胶囊输入框，带账号 / 锁图标）。

记住密码（勾选后本机安全存储账号与密码，便于下次回填）。

登录主按钮；注册账号次按钮。

用户协议、隐私政策提示。

从 Press 进入时可“稍后再说”，返回 Drafts。

10 注册页（本版替代验证码页）

账号（手机号或邮箱）。

密码与确认密码。

错误提示与格式校验。

注册成功后创建托管数字资产账户。

11 账户创建完成

头像占位与默认昵称。

“数字资产账户已创建”。

隐私提示：钱包密钥由系统安全托管。

数字资产账户地址缩写。

账号恢复方式确认。

“继续写入声片”。

若账号已存在，则直接恢复原 Press 任务，不展示重复成功页。

7.5 Press

12 写入方式选择

当前声音视觉、名称、分类和时长。

手机 NFC 写入：兼容性、开启状态、操作说明。

硬件设备写入：设备名称、连接状态、连接入口。

13 声片检测

NFC 检测动画与操作示意。

“将手机靠近声片”。

检测结果：空白、已绑定、无法识别、连接中断。

空白声片编号。

14 永久绑定确认

声音视觉与名称。

声片编号。

写入方式。

明确警示：一张声片只绑定一个声音，完成后不可替换或覆盖。

确认勾选。

“确认并写入”。

“返回检查”。

15 写入与上链进度

声音视觉。

分阶段进度：

写入声片。

校验声片。

创建数字资产。

上链。

加入 Collection。

当前步骤和状态说明。

NFC 阶段保持手机靠近的提示。

网络异常与后台通知说明。

16 完成或异常

成功时显示：

成功反馈。

声音视觉与名称。

声片编号。

数字资产编号。

绑定与上链时间。

“查看 Memory”。

“返回 Collection”。

异常时显示：

失败发生的阶段。

已完成的不可逆步骤。

安全的下一操作。

NFC 已成功时只允许重试上链。

7.6 Collection 与 Memory

17 Collection 列表

顶部：标题、收藏数量、账号头像。

搜索、分类 / 时间 / 地点筛选。

网格 / 时间轴视图切换。

收藏卡片：

声音视觉主图。

名称。

播放按钮。

分类。

录制日期与地点。

声片绑定标识。

未登录状态：登录后查看数字收藏。

已登录无资产：引导从 Drafts 写入第一张声片。

18 Memory 回声详情

动态声音视觉主图。

播放 / 暂停、进度、当前时间与总时长。

名称、分类、描述。

录制日期、时间和地点。

声片编号、写入日期、永久绑定状态。

NFT 数字资产编号、上链时间、链上状态。

折叠的技术信息：区块链网络、合约地址、Token ID、交易凭证。

分享记忆、生成分享卡片、保存视觉图像。

原始音频、时间地点和绑定关系不可修改。名称、分类与描述可作为 App 展示信息更新，但不改变原始链上资产。

7.7 Account 个人中心

19 Account 首页

头像、昵称、账号标识。

登录手机号或邮箱的脱敏信息。

Collection 收藏数量。

已绑定声片数量。

数字资产账户状态。

本地 Draft 与云端同步状态。

最近收藏预览。

入口：

我的数字资产。

声片与设备。

账号与安全。

权限与隐私。

通知设置。

帮助与反馈。

关于 SoundPola。

退出登录。

不建议在个人中心展示性别、生日、职业等与声音收藏无关的信息。

20 我的数字资产

NFT 资产总数。

资产缩略图、声音名称、Token ID 简写、上链状态。

账户地址，默认缩写显示并支持复制。

查看公开链上记录。

外部钱包连接或资产迁移，作为后期高级功能。

该页面是“资产凭证管理”，Collection 仍是以声音记忆为中心的浏览空间。

21 声片与设备

已绑定声片列表。

声片编号、对应声音、绑定时间和状态。

手机 NFC 状态。

已连接的 Press 硬件设备。

智能戒指的连接、电量和录音触发设置。

22 账号与安全

头像与昵称。

已绑定手机号 / 邮箱。

第三方登录绑定。

登录设备管理。

账号恢复方式。

钱包托管与恢复说明。

生物识别或设备锁验证，用于敏感操作。

注销账号。

注销前必须明确处理：

本地 Drafts 是否导出或删除。

Collection 中链上 NFT 无法从区块链删除。

App 账号、云端音频和展示信息的删除范围。

NFT 的保留、转移或失去访问权风险。

23 权限与隐私

麦克风、定位、NFC、蓝牙、通知权限状态。

音频保存位置与同步状态。

“原始录音是否上传”的明确说明。

数据导出。

隐私政策与用户协议。

8. Flutter 页面与路由规划

建议使用声明式路由管理主流程。路由名称仅用于开发约定，UI 文案继续使用产品命名。

模块

建议路由

页面

Shell

/record /drafts /collection

三项底部导航容器

Capture

/recording /record/result/:draftId

录音中、录音结果

Drafts

/drafts/:draftId

暂存详情

Auth

/auth/login /auth/register /auth/account-ready

登录、注册、账户创建完成

Press

/press/:draftId/method

写入方式

Press

/press/:draftId/detect

声片检测

Press

/press/:draftId/confirm

永久绑定确认

Press

/press/:draftId/progress

写入与上链

Memory

/memory/:assetId

回声详情

Account

/account

个人中心

Account

/account/assets

我的数字资产

Account

/account/devices

声片与设备

Account

/account/security

账号与安全

Account

/account/privacy

权限与隐私

8.1 Flutter 模块边界

lib/
├── app/                 # App、主题、路由、全局依赖
├── core/                # 权限、网络、存储、错误、通用组件
├── features/
│   ├── auth/            # 登录、验证、账号恢复
│   ├── record/          # 录音与实时可视化
│   ├── drafts/          # 本地暂存与编辑
│   ├── press/           # NFC / 硬件写入与校验
│   ├── collection/      # 云端收藏列表
│   ├── memory/          # 单条声音详情
│   ├── assets/          # NFT 与链上凭证
│   ├── account/         # 个人中心、隐私、安全
│   └── devices/         # 声片、智能戒指、写入设备
└── shared/              # Design Tokens、模型与共享服务

8.2 状态与数据源

每个页面至少处理四类状态：

loading：加载或恢复任务。

content：正常内容。

empty：没有 Draft、Collection 或设备。

error：权限、网络、写入、上链或账号异常。

数据源按职责拆分：

本地数据库：Draft、录音文件索引、未完成 Press 任务。

安全存储：登录令牌、账号凭证与敏感标识。

云端接口：AdventureX Cloud Media（UserToken、上传/轮询/列表）；契约 `docs/openapi-cloud-media.yaml`。基址可用 `--dart-define=CLOUD_MEDIA_BASE=` 覆盖。

原生能力桥接：麦克风、NFC、蓝牙、定位与触觉。

链上服务：Mint、状态查询和凭证读取，由服务层封装，不直接进入页面组件。

8.3 关键恢复规则

App 被系统中断后，未完成录音应尽可能恢复临时文件。

NFC 写入成功后立即本地落盘绑定结果，再发起上链。

上链失败只重试上链，不得再次写入同一声片。

退出登录不删除本地 Draft；再次登录时明确询问是否归入当前账号。

更换账号时不自动把本地 Draft 合并到新账号。

9. 完整页面层级

App
├── 启动与首次引导
│   ├── 启动页
│   ├── 产品引导
│   └── 权限申请
├── Record
│   ├── 录音首页
│   ├── 录音中
│   └── 录音结果
├── Drafts
│   ├── 暂存列表
│   └── 暂存详情
│       └── Press
│           ├── 登录触发（未登录时）
│           ├── 写入方式
│           ├── 声片检测
│           ├── 永久绑定确认
│           ├── 写入与上链
│           └── 完成 / 异常
├── Collection
│   ├── 网格 / 时间轴
│   └── Memory
│       ├── 播放与记忆信息
│       ├── 声片信息
│       ├── 数字资产信息
│       └── 分享
└── Account
    ├── 个人中心
    ├── 我的数字资产
    ├── 声片与设备
    ├── 账号与安全
    └── 权限与隐私

10. MVP 边界建议

若当前开发版本只验证实时录音与可视化，可暂不开发账号和 NFT；但当前信息架构应预留账号入口和声音的 ownerId、同步状态、声片绑定状态及链上状态。

若当前版本已包含 Press 与 NFT，则账号体系是必要功能，至少需要：

账号密码登录 / 注册。

自动创建或关联数字资产账户。

Account 首页。

我的数字资产（可后续扩展）。

账号与安全（可后续扩展）。

权限与隐私（可后续扩展）。

附录 · 当前实现进度（Flutter · mobile/）

已落地：

全站深色主题（#000000 + #63E0CB），对齐 designstyle.md V2。

声音视觉：K=6 单一 Seed 有机线束；Isolate 音频分析（RMS/频段 ~28Hz、Pitch ~20Hz）产出归一化特征；渲染层 60FPS 在目标控制点间插值；Canvas 旋转复制六次；RepaintBoundary 隔离；High/Medium/Low 动态画质（掉帧时关 Bloom→减粒子→降分辨率→降帧率）；待机停分析并约 24FPS。

三 Tab：Record / Drafts / Collection；Press / Memory / Account 不进底栏。

一级页右上角 Account 头像入口。

账号密码注册 / 登录（本地账号库 + 安全 session）；登录后映射签发 Cloud Media UserToken（SecureStorage，一次下发长期持有）。

Press：multipart 上传源音频 → 轮询至 READY（FAILED 可 retry）→ NFC 写入 `contentId` + `nfc_url`（兼容 Trigger）→ 本地仍模拟 NFT 上链。

Drafts 保持本地优先；Collection 登录后 `listOwnedContents` 同步 READY 内容。

真实麦克风录音、本地播放、NFC 读写（设备支持时）；云媒体契约见 `docs/openapi-cloud-media.yaml`。

Account 首页：昵称、收藏/声片数量、数字资产账户缩写、退出登录（不清 Drafts）。

待后续：启动引导全套、地点 GPS、Account 子树完整页、真实链上 Mint（本 OpenAPI 不含 NFT）。

11. UI/UX 验收要点

未登录用户可以在不受打扰的情况下完成录音并保存 Draft。

任何 NFT 创建入口都会先确认账号归属。

登录完成后返回原 Press 任务，而不是回到首页。

Account 可清楚回答“我是谁、我的资产在哪里、绑定了哪些声片、如何恢复账号”。

Collection 与“我的数字资产”职责不同：前者面向记忆浏览，后者面向所有权凭证管理。

NFC 成功、上链失败、离线、权限拒绝和账号切换均有独立状态。

页面不要求用户理解 Gas、Mint、私钥或助记词才能完成核心流程。