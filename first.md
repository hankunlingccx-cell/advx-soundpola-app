文档用途：Flutter App UI/UX 设计、原型制作、路由规划与开发拆分版本：V2.0视觉基调：黑色背景，主题色 #63E0CB核心对象：声音、声音视觉、NFC 声片、NFT 数字资产、用户身份技术基线：Flutter，Android 优先，并预留 iOS 适配

1. 产品核心规则

SoundPola 将一段声音从“即时捕捉”转化为“可收藏、可触碰、可回看的声音记忆”。

录音完成后，声音先进入 Drafts，不直接进入 Collection。

只有同时完成 NFC 声片写入和 NFT 上链，声音才进入 Collection。

一张 NFC 声片只对应一个声音，写入后永久绑定，不可覆盖或替换。

稀有度属于实体声片，由工厂写入 NFC，不根据录音内容随机生成，也不由 App 判定。录音决定卡片的声音视觉；实体声片决定卡片的稀有度；两者永久绑定后，共同生成最终数字声卡。

录制日期、时间、地点、设备和时长由系统自动记录。

用户只编辑声音名称、分类和描述文本，其中描述为选填。

Record 与 Drafts 需登录后使用（本版启动后先完成登录，再进入主流程）。

首次执行 Press、创建 NFT、同步或恢复资产时必须登录。

NFT 必须归属于一个稳定账号；App 可在后台为普通用户创建托管钱包，不强迫用户理解助记词、Gas 或链上术语。

Drafts 以“本地优先”保存；Collection、NFT 所有权与绑定记录以账号云端数据为准。

登录是 Press 的前置条件，但登录成功后必须回到原声音与原操作节点，不让用户重新寻找内容。

2. 核心对象与归属关系

对象

未登录时

登录后

是否可修改

原始声音

本地保存

可按策略备份到云端

写入声片后不可替换

声音视觉

本地生成

与声音一同同步

成为 NFT 后保留原始版本

名称 / 分类 / 描述

本地编辑

同步至账号

可修改 App 展示信息

时间 / 地点 / 时长

系统自动记录

同步至账号

不可编辑

NFC 声片绑定

不允许创建

归属于当前账号

永久绑定，不可覆盖

NFT 数字资产

不允许创建

归属于数字资产账户

链上记录不可删除或修改；稀有度与实体声片一致

稀有度（声片出厂）

Draft 阶段为「待揭晓」

绑定后写入 NFT 元数据

用户不可编辑或升级

2.1 稀有度规则（不可变）

每枚实体声片出厂时，NFC 预写入：声片唯一编号、稀有度等级、系列／批次、防伪校验信息、初始绑定状态（未绑定／已绑定）。

稀有度体系（前期）：N 普通 · R 稀有 · SR 超稀有 · SSR 极稀有。UR（隐藏限定）预留，本版不启用。

等级视觉：各等级卡面叠加逼真全息箔片（`RarityHoloOverlay`）——虹彩底膜（Sweep 干涉）＋斜向衍射光栅＋RGB 色散镜面扫光＋等级微闪＋边缘菲涅尔焦散；等级越高光谱越宽、光栅更密、扫光更快更炫。N 银灰冷箔弱虹彩；R 薄荷青箔＋冷蓝折射／轻微闪；SR 青→蓝→紫棱镜箔＋双高光／边缘焦散／次描边；SSR 全光谱箔＋密集微闪＋强边缘彩虹焦散，并独占外围反馈（不改声片尺寸）：边缘微粒逸散、拖动／堆叠薄片视差、播放态进度环外侧能量环；鉴定揭晓为 SSR 时终章粒子与加强触觉。贴图可按等级配置，稀有度即时识别以全息箔色与附加效果为准。

不可变约束：

- 稀有度由工厂写入实体声片
- 用户不能编辑或升级稀有度
- 稀有度不根据录音质量、时长或内容计算
- App 不能只相信普通 NFC 文本字段，须校验防伪签名
- 同一枚声片绑定前后稀有度保持一致
- NFT 稀有度必须与实体声片一致
- NFC 写入成功但上链失败时，保留已识别稀有度，只重试上链
- 已绑定声片再次读取时，显示原声音卡片，不能重新生成其他稀有度

生成时机：

1. 录音完成：生成基础声卡（声音视觉、图案、名称／分类／描述、时间地点时长）；稀有度状态为「待揭晓」；保存至 Drafts，不显示正式等级。
2. Press 读取声片：识别编号、读取出厂稀有度、校验真实性 → 进入「声片鉴定」揭晓页 → 确认永久绑定 → 写入 NFC 与 NFT 元数据 → 加入 Collection。

若产品在录音前已读取声片，亦可在录音完成页直接揭晓；本版默认在 Press 检测后揭晓。

最终体验：声音赋予卡片独一无二的图案，声片赋予与生俱来的稀有度；两者永久绑定，才生成完整 SoundPola 收藏声卡。

3. 一级导航

底部导航四个入口（不增加独立“我的”Tab）：

一级导航

核心任务

主要内容

Record

捕捉声音

录音、实时声音可视化、录音控制

Drafts

管理未实体化的声音

播放、删除、编辑基础信息、发起 Press

Collection

浏览已收藏的声音

已写入声片并完成上链的声音收藏

Lab

声音实验室

点选音源、生成并试听阿卡贝拉编排

辅助入口：

Account：通过各一级页面右上角的头像进入。

Press：从 Drafts 卡片「发送至设备写入」、暂存详情或 Press 方式选择页发起。

分类声片播放页：从 Collection 分类跑道圆进入（不进底栏）；页内完成浏览、播放与完整记忆展示，不再设独立 Memory 页。

这种结构让“捕捉—暂存—收藏”始终是产品主叙事；Lab 作为创作实验入口进底栏；账号与 NFT 管理作为支撑层存在。

4. 登录策略

4.1 启动登录（本版）

进入 App 主流程前必须完成账号密码登录或注册。

启动页检查登录态：已登录直接进入主 Tab；未登录进入登录页。登录页不可跳过。

麦克风权限在开始录音时按需申请（不再作为启动门槛）。

退出登录后返回登录页；本机 Drafts 按账号隔离暂存（换号不可见他人草稿／收藏），同一账号再次登录可恢复本机 Drafts。Collection 以云端同步为准。

（产品远期仍可支持 Record / Drafts 本地未登录使用；本版按「先登录」落地。）

4.2 必须登录的触发点

启动进入主 Tab。

点击“Press / 写入声片”。

创建 NFT 或查询链上状态。

进入 Collection 同步资产。

恢复旧设备中的声音收藏。

分享需要验证所有权的链上收藏。

4.3 推荐登录方式

MVP 优先：

手机号或邮箱作为账号标识，配合密码登录 / 注册（本版不做验证码）。

支持「记住密码」：勾选后将账号写入本地偏好、密码写入安全存储，下次打开登录页自动回填；取消勾选则清除。

登录后由系统自动创建或关联托管数字资产账户。

后续可选：

Apple / Google 登录，可按 Android 发布地区选择。

连接外部钱包。

导出或迁移钱包。

界面默认使用“数字资产账户”“链上凭证”，不把“钱包地址、Gas、Mint”等专业词放在主流程。

4.4 登录拦截与任务恢复

从 Draft 详情点击 Press 时：

系统保存当前 draftId、写入入口和已选写入方式。

未登录则以全屏页或底部弹层进入登录。

登录成功后自动创建或关联数字资产账户。

返回原 draftId 对应的 Press 流程，并恢复到登录前步骤。

用户取消登录则返回原 Draft 详情，声音和填写内容不丢失。

登录、密码校验、网络异常均不能清空当前录音或 Draft。

5. NFT 所需个人信息边界

5.1 MVP 必要字段

信息

必要性

用途

展示位置

用户 ID

必须，系统生成

账号与资产归属

不直接展示

手机号或邮箱

至少一项

登录、找回、重要通知

账号与安全

昵称

必须，可使用默认值

App 内身份展示

Account

头像

可选

个性化展示

Account

钱包地址

必须，系统生成或关联

NFT 所有权

数字资产账户

用户协议与隐私同意记录

必须

合规与授权记录

权限与隐私

国家 / 地区

条件必需

适配 NFT、服务与合规策略

首次创建资产时按需获取

5.2 不默认收集

性别、生日、职业、公司、兴趣等非必要画像。

身份证件、真实姓名和详细住址。

银行卡或支付信息。

只有当具体链、NFT 发行方式、法币交易或所在地区法规明确要求 KYC 时，才增加“身份认证”独立流程；声音写入与收藏本身不默认要求 KYC。

5.3 个人中心的信息分层

第一层：头像、昵称、收藏数量、声片数量、账号状态。

第二层：我的数字资产、声片与设备、账号与安全、权限与隐私。

第三层：钱包地址、链、合约、Token ID、交易哈希等技术信息。

技术信息默认折叠，避免个人中心变成区块链后台。

6. 全局声音状态

状态

所在空间

用户可执行操作

录音中

Record

暂停、继续、完成、取消

录音结果

Record

试听、命名、分类、描述、重新录制、保存

已暂存

Drafts

播放、编辑、删除、写入声片

待登录

Drafts / Press

登录或返回

声片检测中

Press

取消检测

写入中

Press

查看进度，不可中断

已写入、待上链

Press / Drafts

等待或重试上链，不重新写入

上链失败

Drafts

查看原因、重试上链

已写入并上链

Collection

分类播放、完整记忆与凭证、分享

永久绑定

分类声片播放页

浏览该分类声片、播放、查看记忆与链上凭证

7. 页面清单

7.1 启动与权限

01 启动页

SoundPola 标识与标语水平居中。

黑底声音视觉动效（待机呼吸态，非录音态；轻幅度 idle；居中 220²）。

底部加载指示居中。

登录状态检查：已登录 → 主 Tab；未登录 → 登录页。

本地 Drafts 与账号数据加载状态。

02 首次引导

产品价值：捕捉声音、写入声片、收藏记忆。

麦克风用途说明。

定位用途说明。

“开始使用”。

“已有账号，登录”次要入口。

（本版可暂缓完整首次引导，启动页后直接登录分流。）

03 权限申请

麦克风权限：在用户开始录音时按需请求；拒绝后禁用录音。

定位权限：用于自动记录地点，拒绝后仍可录音。

NFC 权限或系统状态：在 Press 前按需请求。

通知权限：用于上链完成或失败提醒，按需请求。

隐私政策入口。

7.2 Record

04 录音首页

顶部：SoundPola 标识、连接设备图标入口（`ConnectDeviceIconButton` → WiFi 扫码配对）、账户入口。

未授权麦克风（阻断态，非普通空态）：`MIC ACCESS REQUIRED` / 需要麦克风权限；中央为关闭的麦克风端口视觉；主按钮「允许麦克风权限」或「前往系统设置」。

已授权待机（非空态）：四象限严格镜像线性声场（仅生成 Q1 纯圆形粒子线性排列，其余三象限由 Canvas scale 镜像；待机缓慢折叠漂移）；`LISTENING STANDBY` / 等待捕捉声音；录音按钮为页内唯一主操作（60dp 实心青圆＋黑点，录音时平滑变为圆角方停止符）。主操作下方提供次级文字入口「导入本地音频」（不抢主按钮焦点）；无麦克风权限时亦可导入。

主录音按钮靠底部导航上方；与可视化间距缩短；极细垂直能量线关联（非分割线）。视觉中心约在屏高 39%–43%，主体宽约屏宽 74%–82%。

底部四项导航（Record / Drafts / Collection / Lab）；底栏降低高光／边框与非选中对比度，焦点依次为可视化 → 录音钮 → 状态文 → 导航。

04b 导入本地音频

从系统文件选择器选取本地音频（m4a／mp3／wav／aac／ogg／flac／caf）。复制至应用 `recordings/`；探测时长；不足 3 秒拒绝；超过 30 秒按 bake 上限截断会话时长。生成 visualSeed 与确定性合成 AudioDrive 时间序列（导入暂不做真 PCM 分析），进入结果页（标题「导入结果」，名称预填文件名），保存路径与录音结果一致 → Drafts。

外部指环录音：已蓝牙配对的指环在实体按键结束录音后，App 自动经 BLE 下载 Speex → 本机解码为 WAV（可试听）→（尽力）上传云端 → 进入结果页（标题「指环录音完成」），不经过「录音中」页。

05 录音中

关闭 / 取消入口。

实时声音可视化：参考 visuallization Axis Field 线性声场，Flutter 端为四象限严格镜像。几何只在第一象限计算并绘制一次（纯 `drawCircle` 圆形粒子：径向／弧向／弦向平行珠串），其余三象限用 Canvas `scale(±1,±1)` 镜像，不重复生成粒子。Isolate 输出 `AudioFeatures`；CustomPainter 只读快照。**主驱动为音量**：小声→运动慢、形变小；大声→运动快、形变大；`volumeStyle` 随响度在安静／中等／响亮样式 A／B／C 间连续变形（尖锐度、密度、珠径性格、波纹／弦向结构、流速、青→粉着色）；频谱／onset 仅作轻辅。禁止单纯放大缩小或整图 scale。整体外轮廓几乎固定；粒子半径仅随样式性格变化。待机持续极慢漂移。禁止球体点阵、线段／非圆粒子、按阈值硬切离散图案、每帧随机闪烁。

录音计时；最长 30 秒，到达上限自动完成录音。

录音中 / 已暂停状态。

暂停 / 继续。

完成录音（须等麦克风启动成功后方可点按；启动中显示「正在启动…」，失败可重试）。

时长不足 3 秒：不直接丢弃文件，展示 `SOUND TOO SHORT`；主按钮「重新录音」，次按钮「仍然保存」。

音量过低、过高或正常提示。

录音文件写入应用文档目录 `recordings/*.m4a`（AAC）；`stop` 若未返回路径则回退至启动时路径并确认文件已落盘。实时特征由幅度流驱动 Isolate（避免卡死于部分机型的 `startStream`）。存量包可读 `audio.wav`／`audio.m4a`。

06 录音结果

试听、命名、分类、描述、重新录制。

进入结果页即分配 `sounds/{id}/`：复制音频与 AudioDrive 特征时间序列，并**立即**后台离屏 bake（不阻塞命名／保存）。以同一 `visualSeed`＋特征确定性重绘，输出 12fps Indexed-MJPEG（`visual.mjpg`＋`visual.idx`＋`visual_manifest.json`）与 `cover.jpg`；Android 再尽力编码同内容 H.264 `visual.mp4`（云端上传用，本机播放仍走 MJPEG）。状态：`processing_visual` → `indexing` → `ready`／`failed`（失败保留音频并可重试，不删录音）。「保存至 Drafts」只写入元数据并提交已准备好的包；重新录音／放弃结果页则删除未提交包。bake 就绪后（已登录）后台预上传云端；Press／云上传仍经 `ensureReady` 兜底等待已启动的 bake。

播放（Draft 详情／分类 Memory）优先按音频时间轴索引解码已留存 JPEG 帧；算法升级后旧记录仍用原帧流（`rendererVersion`）。

自动信息：日期、时间、地点。

名称：必填，建议 20 字以内。

分类：必选；用户自定义，可新建（本版不提供删除分类）。

描述：选填。

“保存至 Drafts”。

7.3 Drafts

07 Drafts 列表（声音封存工作台）

视觉定位：Y2K 数字设备 × 冷白 ABS 工程塑料 × 黑色玻璃屏；背景 #030505；禁止陶瓷／香皂感与大面积白晕。

顶部：主标题 `Drafts` +「待封存的声音」；数量胶囊；账户入口。

拟物设备：冷白 ABS（#F3F5F2／#C9CECB）、内凹前面板、黑色玻璃屏、凹陷插槽嵌于外壳顶缘（自上向下插入）、状态灯；机身下方叠一层更高的毛玻璃存储仓。声卡插入仅进入待写入态；NFC 写入并封存成功后，带稀有度贴图的圆形声片（N／R／SR／SSR 固定贴图，与 Collection 同源）落入仓内，模拟重力下落、地面／侧壁／声片两两碰撞回弹后静止（中心距约束，不允许完全重合叠成一张）；仓内可同时保留多张（至多 5 张、各保留约 7 天），数据本地存储但按登录账户隔离（换号不可见他人仓内声片），空仓无装饰。待机：`READY TO PRESS`／`SELECT A SOUND CARD`；靠近：`CARD DETECTED`／`RELEASE TO INSERT`；写入：`PRESSING MEMORY`＋进度；完成：`MEMORY PRESSED`。拖拽仅跟手；松手进入释放区后对齐顶部插口并下吸入槽（遮挡仅发生在插入动画，口下被机身裁切）。设备整体放大，约占屏高约 38%。

NFC 指引：插入成功后展开；Browsing 不提前展开。流程：录音可视化 bake 完成后后台预上传云端（Draft 仍为本地暂存）→ 若尚未 READY 则在 Press 时补齐上传／处理 → 单次贴近**先识别是否已写入**（MIME／Text／URI `/c/{contentId}` 或出厂 bound）→ 空白才写入 → 上链（无需贴近）。云端失败与 NFC 写入失败分开引导重试。若贴近到已有内容的声片：系统提示「该声片已写入」，不可覆盖，并引导换空白声片；此时云端声音内容已保留，点「换空白声片继续写入」直接进入 NFC 写入，不重新上传或生成。

暂存卡组：横向错位堆叠；冷白半硬质卡面与设备同材质系统；含 DRAFT 编号、圆形视觉、分类色带。

底栏：悬浮磨砂控制条，无描边黑边，选中青色指示。

08 Draft 详情

声音视觉主图（点击播放／暂停）、名称 / 分类 / 描述（分类为用户自定义：已创建列表或新建）、只读元数据。

暂存声音操作项：

- 播放
- 编辑声音信息
- 删除（已写入 NFC 待上链的声音不可删除）
- 发送至设备写入

主操作：「发送至设备写入」→ 进入硬件写入任务页。APP 只发送写入任务，不在暂存页完成写入；放入声卡、确认写入与硬件校验均在设备端完成。

Drafts 列表对当前选中声卡亦提供「发送至设备写入」快捷入口；手机 NFC 封存仍可通过长按拖入拟物设备完成。

7.4 登录与账户建立

09 登录页 / 登录触发页

启动进入或从 Press 进入时显示；Press 场景不中断用户对当前声音的确认。

品牌名 SoundPola（大号标题）。

从 Press 进入时：当前待写入声音的缩略图与名称。

说明：“登录后，这段声音将归属于你的数字收藏。”（启动登录用“登录后开始捕捉声音…”）

手机号 / 邮箱 + 密码（胶囊输入框，带账号 / 锁图标）。

记住密码（勾选后本机安全存储账号与密码，便于下次回填）。

登录主按钮；注册账号次按钮。

用户协议、隐私政策提示。

从 Press 进入时可“稍后再说”，返回 Drafts。

10 注册页（本版替代验证码页）

账号（手机号或邮箱）。

密码与确认密码。

错误提示与格式校验。

注册成功后创建托管数字资产账户。

11 账户创建完成

头像占位与默认昵称。

“数字资产账户已创建”。

隐私提示：钱包密钥由系统安全托管。

数字资产账户地址缩写。

账号恢复方式确认。

“继续写入声片”。

若账号已存在，则直接恢复原 Press 任务，不展示重复成功页。

7.5 Press

12 写入方式选择

当前声音视觉、名称、分类和时长。若声音已不存在：`NO SOUND TO PRESS`，引导返回录音。

手机 NFC 写入：兼容性、开启状态、操作说明。

硬件设备写入：设备名称、连接状态、连接入口。

12b 发送至设备写入（硬件路径）

流程：

选择暂存声音 → 点击「发送至设备」→ 选择或确认已连接设备 → APP 发送声音写入任务 → 设备接收并缓存任务 → 用户在设备上放入声卡 → 设备执行写入与校验 → 结果同步回 APP → 写入成功后进入 Collection。

职责边界：APP 发送的是写入任务，不会直接在暂存页完成写入；插入声卡、确认写入和硬件校验均在设备端完成。设备应将等待声卡、写入中、校验中、成功或失败等状态实时同步至 APP。写入成功后，该声音从「暂存」更新为「已写入」并进入 Collection；写入失败时保留原暂存录音，允许用户重新发送任务或在设备端重试。

点击「发送至设备」后，APP 依次显示：

| 状态 | APP 提示 |
| --- | --- |
| 未连接 | 请先连接 SoundPola 设备 |
| 正在发送 | 正在向设备发送写入请求 |
| 设备已接收 | 请求已发送，请前往设备继续操作 |
| 等待放入声卡 | 请在设备中放入一枚空白 Sound Piece |
| 正在写入 | 设备正在写入，请勿移走声卡 |
| 正在校验 | 正在验证声卡绑定结果 |
| 写入成功 | 声音已写入并加入收藏 |
| 写入失败 | 写入未完成，可重新发送或重试 |

13 声片检测

NFC 检测动画与操作示意。

“将手机靠近声片”。

检测结果：空白（未写入正版声片）、已写入、无法识别、防伪失败、连接中断。

已写入：提示「该声片已写入」，不可覆盖；引导换空白声片继续写入。若当前声音的云端内容已就绪，跳过重新上传与生成，直接进入写入贴近。

读取出厂编号与系列；稀有度暂不明文展示，进入揭晓页。

开发态：若芯片尚无工厂 MIME，可按 tagId 派生演示出厂档案（带演示签名）；生产须使用工厂预写芯片。

13b 稀有度揭晓（声片鉴定）

纯黑背景。中央显示声音生成的圆形声卡：由暗色轮廓逐渐显现；NFC 扫描纹理围绕声卡运动；稀有度字样（N→R→SR→SSR）逐级点亮；最终显示该声片等级并套用对应颜色／材质／动态效果；轻微震动反馈。若最终为 SSR：终章粒子爆发 + 加强触觉，卡面保持镭射与微粒呼吸。

文案示例：

声片鉴定完成

SSR · 极稀有

该稀有度来自声片 No. SP-0827

“该稀有度由实体声片决定”

主按钮：「确认并永久绑定」；次按钮：「返回检查」。

稀有度可在永久绑定前揭晓，但只有绑定成功后才正式归属于该声音卡片。

14 永久绑定确认

声音视觉与名称。

声片编号、已揭晓稀有度、系列。

写入方式。

明确警示：一张声片只绑定一个声音，完成后不可替换或覆盖；稀有度由实体声片决定，不可编辑。

确认勾选。

“确认并永久绑定”。

“返回检查”。

15 写入与上链进度

声音视觉。

分阶段进度：

写入声片。

校验声片。

创建数字资产。

上链。

加入 Collection。

当前步骤和状态说明。

NFC 阶段保持手机靠近的提示。

网络异常与后台通知说明。

16 完成或异常

成功时显示：

先播放一段约 0.9–1.2 秒的 3D 卡片翻面动画（正面为深色玻璃／封片质感，隐去内容，仅露出「SoundPola」品牌字样；背面为 NFT 稀有度卡）；翻面完成后触觉反馈一次，随后 NFT 卡永久展示，不再翻回。

NFT 稀有度卡（`SoundNftCard` 组件，Press 完成页与分类播放页分享卡共用）：顶部稀有度角标（N／R／SR／SSR）＋「NFT」小标签；居中圆形声音可视化（`SoundVisualCanvas`）；链上变体可叠分级镭射与稀有度描边光晕；**分享变体中部仅为纯可视化，无外圈描边、无镭射／光晕／叠色**。标题与分类；细分隔线下方为声片编号、数字资产编号（缩略显示）、上链时间，字距拉宽、色阶为三级文字色。

“返回 Collection”与“查看声片记忆”（进入该声音所属分类的声片播放页）在翻面完成后渐显，翻面进行中不可点击。

异常时显示：

失败发生的阶段。

已完成的不可逆步骤。

安全的下一操作。

NFC 已成功时只允许重试上链。

7.6 Collection 与分类声片播放页

17 Collection 列表

顶部：标题、收藏数量（与分组数）、账号头像。

布局：按分类聚合为分组胶囊，双列瀑布流排布（短列优先落入下一组）。SSR 声片在胶囊内有边缘微粒与浮起视差，尺寸与其他等级相同。

分组胶囊（玻璃拟物）：半透明玻璃跑道；组内声片按稀有度显示固定贴图（N／R／SR／SSR，`disc_n/r/sr/ssr.png`），全站复用。

交互：

点击分类跑道（胶囊空白区或组内圆片）→ 直接进入该分类的「分类声片播放页」；若点的是具体圆片，则以该声片为初始选中项。取消独立 Memory 页，不再二次跳转查看记忆。

长按声卡圆片 → 浮出并拖到其他分类跑道调整展示分类（仅 App 展示信息）。

下拉刷新同步云端收藏。

未登录：`ACCOUNT OFFLINE` 引导登录。同步中：`SYNCING COLLECTION`（不伪造成空收藏）。已登录无资产且无草稿：`COLLECTION EMPTY`。有草稿待封存：`NO ASSETS YET`（显示待封存数量，主按钮前往 Drafts）。分类内无声片见分类播放页条件空态。

18 分类声片播放页

页面定位：同时承担浏览该分类全部声片、播放当前选中声音、展示完整记忆内容、展示实体声片信息、展示 NFT 数字资产信息。从 Collection 分类跑道进入，不进底部导航。纯黑背景，主色 #63E0CB。避免传统音乐播放器／唱片机／光盘 UI（无中心孔、刻痕、固定播放三角）。

最终路径：Collection 分类跑道圆 → 分类声片播放页 → 横向切换声片 → 直接播放并查看完整记忆。不再设置独立 Memory 页面。

18.1 顶部播放区

视觉主体：当前选中声音的四象限线性声场可视化（SoundVisualCanvas，可带进度环），边长约屏宽 78%（上限 360），首屏占主导；播放时随音频变化，点击切换播放／暂停。不在主视觉区重复展示标题／稀有度角标／时长文案，也不再叠放大号贴图声卡（贴图仅出现在横向堆叠，与 Collection 跑道一致）。

页面为整页一体滚动（可视化 + 横向堆叠 + 记忆／声片／NFT），禁止「上方固定、仅下方信息区可滚」的双区滚动。顶栏返回／分享固定；堆叠横向滑动与整页纵向滚动并存。

下方横向堆叠使用跑道贴图，整体尺寸小于可视化主体，仅作切换导航。

标题、分类、描述与元信息在下方记忆卡片中展示与编辑。

暂停后进度停留；播放结束后环形进度回到起点。

切换声片时停止当前声音并加载新声音，禁止重叠播放。

SSR 仅在稀有度为 SSR 时，于进度描边外增加能量环：播放时明显、暂停极弱呼吸，不得影响进度识别。

环形进度与稀有度效果位于贴图独立效果层，不修改原始贴图。

18.2 横向声片堆叠区

该分类全部声片水平排列、部分重叠，形成前后层级的横向三维堆叠。

当前选中位于中心，尺寸最大且完整可见；左右逐渐缩小并被前方遮挡；距中心越远亮度和透明度越低。

不使用等间距横向列表或垂直堆叠。

左右拖动切换；松手吸附至最近声片并轻微触觉反馈；点击非中心声片使其移至中心。

切换后顶部播放区与下方记忆内容同步更新。

18.3 记忆内容区

直接展示完整记忆，不设「查看 Memory」或「进入详情」按钮。

名称、分类、描述（空描述显示「这段声音还没有留下文字记忆。」，可提供「添加描述」入口但不跳新页）。

录制日期、时间、地点、录音时长。

地点：录音完成时由 GPS 定位自动写入（反查地名；失败则坐标或「地点未记录」），只读，不可手改。

名称／分类／描述可作为 App 展示信息编辑（分类选择器展示已创建名称并可新建）；原始音频、录制时间地点、实体绑定不可改；编辑不改变声片贴图与链上资产。

18.4 实体声片信息区

可折叠；默认显示游戏式稀有度展示（大号 N／R／SR／SSR 代号＋中文名＋等级条）与声片编号。稀有度不占用普通键值行。

展开：系列／批次、写入日期、NFC 校验状态。

不展示「绑定状态」字段（写入即永久绑定）。不提供：替换声音、修改稀有度、解除绑定、重新写入。

18.5 NFT 数字资产信息区

默认折叠。默认显示资产编号；展开：上链时间、区块链网络、当前链上状态、交易凭证。

稀有度不在此区重复展示，以实体声片信息区为准（与 NFC 校验一致且不可编辑）。

18.6 声片贴图一致性

每个声音写入声片后按稀有度使用固定贴图（N 黑底、R 薄荷青、SR 紫、SSR 粉；中央星标＋环绕 `CYBERPUNK VIBES.`），用于 Collection 总览、分类播放页横向堆叠、Drafts 仓内声片等。分享／NFT 卡中部仍用声音可视化，不替换为稀有度贴图。

禁止：按 visualSeed 随机换贴图、播放时重生成图案、换页改配色、重建时重新随机、拉伸变形、不同裁切导致主体变化。稀有度未揭晓时回退 N 档贴图。

允许叠加：环形进度、选中光晕、轻微视差、稀有度分级镭射（N 银灰弱扫／R 薄荷／SR 青蓝紫棱镜／SSR 全光谱绚烂）、SSR 边缘微粒／播放外环／揭晓终章粒子、加载与播放反馈——均在贴图上方独立层或外围。

18.7 页面状态

默认：中央选中，环形进度在起点，轻微呼吸，下方完整记忆。

播放：环形描边顺时针增长；外围低强度声音响应；原始贴图不变。

暂停：进度停留；动态减弱；再点从当前位置继续。

切换：堆叠连续移动；顶部可贴图交叉淡化；记忆在吸附后更新；当前音频停止。

空分类：「这个分类中还没有声片。」+「返回 Collection」。

加载失败：保留贴图与记忆；「声音加载失败」+「重新加载」。

18.8 路由与实现要点

`/collection`（主 Tab 内总览）

`/collection/category/:categoryId`（分类声片播放页；`categoryId` 为 Uri 编码的分类名，兼容中文；可带 `soundId` query。实现时 path 参数已由路由解码，勿再 `Uri.decodeComponent`，否则非 ASCII 会触发 Illegal percent encoding。）

删除独立记忆详情路由 `/memory/:assetId`。

Stack 分离贴图、环形进度与稀有度效果层；环形进度更新时仅重绘进度层。横向堆叠基于 PageController + Transform + 按距离排序的 Stack。整页 `CustomScrollView` 一体纵向滚动；顶栏固定。切换时预加载相邻贴图与音频。纯黑背景避免白闪；目标 60 FPS。

18.9 分享当前声片

顶部栏右侧提供「分享」入口（图标＋文字）。点击弹出黑色玻璃底部弹层，内含分享卡预览。

分享卡（`SoundNftCard` · `share` 变体）对齐 Figma 四档视觉（N／R／SR／SSR）：

- 灰边外壳（#272728／#505050）+ 黑底内卡，内描边按稀有度：N 白、R #38D7D0、SR #7454EB、SSR #ED4F8F
- 顶部大号等级字样；N 为白字＋凹口线，R／SR／SSR 为同色顶栏＋梯形缺口
- 中部圆形声音可视化（`SoundVisualCanvas`：无外圈描边、无镭射／光晕／叠色）；稀有度声片贴图仅用于 Collection 跑道／堆叠，不占分享卡中心
- 白底信息板：声音名称、声片编号、地点、录制时间、资产编号条；右侧环绕字印章（CYBERPUNKVIBES.）
- 脚注：`SoundPola` ＋「已写入声片」

下方「保存卡片」（当前为占位：提示分享卡片已备好并给出文字摘要）与「关闭」两个操作。

18.10 分类空态

分类下无声片：`NO SOUNDS IN 「分类名」` / 「这个分类还是空的」，主按钮返回查看全部收藏。不使用「去创建分类」类文案。

7.7 Account 个人中心

19 Account 首页

头像、昵称、账号标识。

登录手机号或邮箱的脱敏信息。

Collection 收藏数量。

已绑定声片数量。

数字资产账户状态。

本地 Draft 与云端同步状态。

最近收藏预览。

入口：

我的数字资产。

声片与设备。

账号与安全。

权限与隐私。

通知设置。

帮助与反馈。

关于 SoundPola。

退出登录。

不建议在个人中心展示性别、生日、职业等与声音收藏无关的信息。

20 我的数字资产

NFT 资产总数。

资产缩略图、声音名称、Token ID 简写、上链状态。

账户地址，默认缩写显示并支持复制。

查看公开链上记录。

外部钱包连接或资产迁移，作为后期高级功能。

该页面是“资产凭证管理”，Collection 仍是以声音记忆为中心的浏览空间。

21 声片与设备

已绑定声片列表。

声片编号、对应声音、绑定时间和状态。

手机 NFC 状态。

已连接的 Press 硬件设备。

硬件扫码配对（Account → WiFi 扫码连接设备）：

设备 LCD 显示二维码 `SNDPOLA1|<ipv4>|<port>|<nonce>|<devid>`；App 摄像头扫描并校验魔数／IPv4／端口／nonce。

App 经局域网原始 TCP Socket 向设备 `POST /pair`（不用 package:http，因嵌入式回包后常 RST，HttpClient 会丢已收响应），JSON 字段：`nonce`、`token`（Bearer）、`user_id`、`email`、`server_url`（当前云端 `CloudMediaConfig.baseUrl`）。

状态码：200 成功；401 二维码过期／已用；403 设备未开配对；400 参数无效；超时／无响应视为不在同一网络。

界面状态：loading →（权限）→ scanning → sending → success；失败可重扫。nonce 一次性且约 3 分钟 TTL；仅配对界面受理；局域网明文 HTTP 为 MVP 可接受，后续可加 TLS。

入口：Record 主页账户旁设备图标；Account 主按钮「WiFi 扫码连接设备」；「我的设备」空态／列表「连接新设备」；Press「硬件设备写入」未绑定时直接进入扫码页。

指环蓝牙配对（同一配对页底部「蓝牙配对指环」→ `/account/pair-ring`）：

- BLE 扫描仅展示广播名称以 `ring` 开头（大小写不敏感）的设备；列表显示名称与 MAC，隐藏其他蓝牙设备。
- 用户点选后 GATT 连接 Nordic UART 协议指环，读取系统信息／电量，写入本机配对地址，并登记到「我的设备」。
- 登录且已配对后，后台 `RingRecordingService` 保持待命：指环实体按键录音结束 → BLE 下载 Speex → 本机解码 WAV →（尽力）上传云端 → 直接进入录音结果页（标题「指环录音完成」），**不走**手机麦克风显式录制页。
- Android 原生桥：`RingSoundBridge`（MethodChannel `soundpola/ring_sound`：`scanRings`／`connectRing`／`receiveNextRecording`／`cancel`）；解码依赖 `jspeex`。

硬件端 Memory Terminal HMI（权威：`hadwareui.md` + `docs/hardware-ui-preview/index.html`）：

- 分辨率 480×320；三层结构：顶部状态轨（PHONE／NET／NFC／HOLO）→ 中央任务舞台 → 底部 IN／NFC／OUT 机械引导托盘。
- **00 配对页**：LCD 展示二维码载荷 `SNDPOLA1|<ipv4>|<port>|<nonce>|<devid>`；与 App Account →「配对 NFC 设备」扫码对接；约 3 分钟 TTL，可刷新；配对中不接收入卡。
- 无模式选择：手机发声写入，或投已绑定卡显影；事件自动推进。
- 状态：00 配对 → 01 待机 → 02／02b 接收就绪 → 03 等待空白卡 → 04 识别 → 05／05b 写入校验 → 07 完成出卡取卡；或 06a／b／c 显影；异常仅 08a 已绑定冲突、08b 凭证延迟（已取消机械卡滞用户页）。
- LCD 黑白青工业语言，禁止声音可视化；球体／全息光谱仅全息装置。与 App Drafts 写入机拟物 UI 视觉分离、任务衔接。

智能指环：蓝牙配对、电量读取、按键录音触发与自动下载（见上「指环蓝牙配对」）；本版不提供独立电量设置页。

22 账号与安全

头像与昵称。

已绑定手机号 / 邮箱。

第三方登录绑定。

登录设备管理。

账号恢复方式。

钱包托管与恢复说明。

生物识别或设备锁验证，用于敏感操作。

注销账号。

注销前必须明确处理：

本地 Drafts 是否导出或删除。

Collection 中链上 NFT 无法从区块链删除。

App 账号、云端音频和展示信息的删除范围。

NFT 的保留、转移或失去访问权风险。

23 权限与隐私

麦克风、定位、NFC、蓝牙、通知权限状态。

音频保存位置与同步状态。

“原始录音是否上传”的明确说明。

数据导出。

隐私政策与用户协议。

8. Flutter 页面与路由规划

建议使用声明式路由管理主流程。路由名称仅用于开发约定，UI 文案继续使用产品命名。

模块

建议路由

页面

Shell

/record /drafts /collection /lab

四项底部导航容器（Lab = Sound Lab）

Capture

/recording /record/result/:draftId

录音中、录音结果

Drafts

/drafts/:draftId

暂存详情

Auth

/auth/login /auth/register /auth/account-ready

登录、注册、账户创建完成

Press

/press/:draftId/method

写入方式

Press

/press/:draftId/detect

声片检测

Press

/press/:draftId/reveal

稀有度揭晓（声片鉴定）

Press

/press/:draftId/confirm

永久绑定确认

Press

/press/:draftId/progress

写入与上链

分类声片播放

/collection/category/:categoryId

浏览／播放／记忆／声片与数字资产一体化

Account

/account

个人中心

Account

/account/assets

我的数字资产

Account

/account/devices

声片与设备

Account

/account/security

账号与安全

Account

/account/privacy

权限与隐私

8.1 Flutter 模块边界

lib/
├── app/                 # App、主题、路由、全局依赖
├── core/                # 权限、网络、存储、错误、通用组件
├── features/
│   ├── auth/            # 登录、验证、账号恢复
│   ├── record/          # 录音与实时可视化
│   ├── drafts/          # 本地暂存与编辑
│   ├── press/           # NFC / 硬件写入与校验
│   ├── collection/      # 云端收藏列表与分类播放
│   ├── assets/          # NFT 与链上凭证
│   ├── account/         # 个人中心、隐私、安全
│   └── devices/         # 声片、智能戒指、写入设备
└── shared/              # Design Tokens、模型与共享服务

8.2 状态与数据源

每个页面至少处理四类状态：

loading：加载或恢复任务。

content：正常内容。

empty：没有 Draft、Collection 或设备。

error：权限、网络、写入、上链或账号异常。

数据源按职责拆分：

本地数据库：Draft、录音文件索引、未完成 Press 任务。新账号本地声音列表为空（无内置演示条目）；仅在用户录音入库或云端同步 READY 资产后出现内容。

安全存储：登录令牌、账号凭证与敏感标识。

云端接口：AdventureX Cloud Media（UserToken、上传/轮询/列表）；契约 `docs/openapi-cloud-media.yaml`；预设基址 `http://soundpola.babelbeast.com:9000`，可在登录页「服务器设置」运行时改写。

原生能力桥接：麦克风、NFC、蓝牙、定位与触觉。

链上服务：Mint、状态查询和凭证读取，由服务层封装，不直接进入页面组件。

8.3 关键恢复规则

App 被系统中断后，未完成录音应尽可能恢复临时文件。

NFC 写入成功后立即本地落盘绑定结果，再发起上链。

上链失败只重试上链，不得再次写入同一声片。

退出登录不删除本机为该账号暂存的 Draft；换号不可见。再次登录同一账号后恢复本机 Drafts。Collection 仅显示当前账号云端资产。

更换账号时不自动把本地 Draft 合并到新账号。

9. 完整页面层级

App
├── 启动与首次引导
│   ├── 启动页
│   ├── 产品引导
│   └── 权限申请
├── Record
│   ├── 录音首页（含导入本地音频次级入口）
│   ├── 录音中
│   ├── 导入本地音频 → 结果页
│   └── 录音结果
├── Drafts
│   ├── 写入工作台（机器 + 暂存托盘）
│   └── 暂存详情
│       └── Press
│           ├── 登录触发（未登录时）
│           ├── 写入方式
│           ├── 声片检测
│           ├── 稀有度揭晓
│           ├── 永久绑定确认
│           ├── 写入与上链
│           └── 完成 / 异常
├── Collection
│   ├── 分类胶囊瀑布流
│   └── 分类声片播放页
│       ├── 当前声片播放与环形进度
│       ├── 横向三维声片堆叠
│       ├── 完整记忆内容
│       ├── 实体声片信息
│       ├── NFT 数字资产信息
│       └── 分享与编辑展示信息
├── Lab（Sound Lab 声音实验室，底部第四 Tab）
└── Account
    ├── 个人中心
    ├── 我的数字资产
    ├── 声片与设备
    ├── 账号与安全
    └── 权限与隐私

9.1 Sound Lab 声音实验室

定位：自动化声音阿卡贝拉页；底部导航第四入口（标签 Lab）；旁路路由 `/lab/sound` 仍可用。

**用户流程**：点选 1–4 段声音 →「生成阿卡贝拉并试听」→ 截取各段最高音量有效声段（`HotClip`）→ **随机发明节拍型**（如「咚咚打咚咚-」）→ 把声段填进拍点并偶发叠唱试听。

**音源**：当前会话录音、Drafts、Collection。切到 Lab Tab、打开 Lab 或点刷新时：若已登录则先 `listContents` 同步云端收藏；条目缺少本机 `audioPath` 时按 `contentId` 下载云端播放资产（`GET /api/v1/contents/{id}/assets/audio`）缓存到本机后再进入可选列表。Drafts／Collection 本地变更（如保存新录音）会立即重建可选列表（IndexedStack 常驻不丢刷新）。无本地文件且无 `contentId`／未登录则不可选。云端条目的 `visualSeed` 由 `contentId` 稳定派生，避免批量同步时撞成同一图案；Drafts 列表缩略优先显示 bake 后的 `cover.jpg`。

**节拍**：暂不走 AI；由程序 `RhythmPattern.invent` 按 seed 随机发明（咚≈重击、打≈轻击、-=休止）。每次生成不同。AI 接入点仍保留在 `beat_ai_api_config.dart`，网关当前为 `LocalBeatGenerationApi`。

**声部默认**：0→lead，1→response，2→pad，3→percussion；切片落在各自 HotClip 内；同时最多 2 层。

**AI（暂关）**：日后将 `BeatGenerationApiGateway.instance` 改回 `AiBeatGenerationApi()`，并配置 `beat_ai_api_config.dart`。

实现：`mobile/lib/lab/`、`mobile/lib/screens/lab/sound_lab_screen.dart`。

10. MVP 边界建议

若当前开发版本只验证实时录音与可视化，可暂不开发账号和 NFT；但当前信息架构应预留账号入口和声音的 ownerId、同步状态、声片绑定状态及链上状态。

若当前版本已包含 Press 与 NFT，则账号体系是必要功能，至少需要：

账号密码登录 / 注册。

自动创建或关联数字资产账户。

Account 首页。

我的数字资产（可后续扩展）。

账号与安全（可后续扩展）。

权限与隐私（可后续扩展）。

附录 · 当前实现进度（Flutter · mobile/）

已落地：

全站深色主题（#000000 + #63E0CB），对齐 designstyle.md V2。

声音视觉：四象限镜像线性粒子线束（Q1 纯圆粒子确定性生成一次 → Canvas scale 镜像三象限）；参考 visuallization 线性万花筒密度与流动感；Isolate AGC＋spectrum 局部映射；CustomPainter + RepaintBoundary。**性能**：idle／complete／列表缩略静态单帧（不停 60fps ticker）；仅录音／当前播放／暂停保留动画，并按实际帧率分档降粒子与目标 fps；全息／SSR 粒子仅主卡或选中卡；播放 Indexed-MJPEG 解码去重＋小 LRU／封面兜底；录音特征 UI 约 25Hz；大面积毛玻璃降低 sigma。录音钮：实心青圆＋黑点，录音变圆角方停止符。

可视化留存：录音中记录 AudioDrive 时间序列；**录音／导入结束进入结果页即**离屏确定性 bake（低粒子档、逐帧向 UI yield）→ Indexed-MJPEG（512²／12fps／q≈80）＋idx＋manifest＋cover；Android 再编码 `visual.mp4`（H.264）供云端。保存 Draft 时提交包元数据，不把 bake 推迟到 Press。播放按 audioPositionMs 随机访问帧（最新帧优先、旧解码丢弃）；`rendererVersion=soundpola_kaleido_linear_v3`（旧包保留既有帧流不改写）。bake 就绪（或失败仅音频）后，若已登录则后台两段式预上传：`POST /contents` 仅音频 →（有 mp4 时）`POST /contents/{id}/video` 字段 `video`；成功常直接 `READY`，写入 `contentId`／`nfc_url` 但不改 Draft 状态；Press 时若已 READY 则跳过上传直接 NFC 写入（未就绪仍经 `ensureReady` 兜底）。

四 Tab：Record / Drafts / Collection / Lab；Press / 分类声片播放页 / Account 不进底栏。

启动页分流：已登录进主 Tab，未登录进登录页；麦克风在录音时按需申请。

一级页右上角 Account 头像入口。

账号密码注册 / 登录（本地账号库 + 安全 session）；登录后映射签发 Cloud Media UserToken（SecureStorage，一次下发长期持有）。

Press：优先复用 Draft 预上传的 `contentId`／`nfc_url`（READY 则跳过音频上传）；若本机仍有 `visual.mp4` 则仍会 `POST .../video` 补传（避免仅音频 READY 导致 NFC 网页 `/preview/{id}/video` 空白）。否则两段式上传——`POST /api/v1/contents` 仅源音频拿 `content_id`（multipart 文件名优先为用户设定的声音名称＋原扩展名，未命名则回退本机 `audio.wav`／`audio.m4a`），再 `POST /api/v1/contents/{id}/video` 上传本机 `visual.mp4`（字段名 `video`；无 mp4 时仅音频并轮询）；视频成功常直接 `READY`。上传后（及本地改名时）`PATCH /api/v1/contents/{id}` 把用户设定的声音名称写入 `display_label`（否则网页默认「声音碎片 #XXXX」）。NFC 写入前先识别声片是否已有内容（绑定 MIME／Text／URI `/c/{id}`／出厂 bound），已有则提示「该声片已写入」并拒绝覆盖；空白才写入 `contentId` + `nfc_url`（兼容 Trigger）→ 本地仍模拟 NFT 上链；可选经已配对 Memory Terminal 硬件完成物理写入／出卡（HMI 见 `hadwareui.md`）。Draft 详情／列表「发送至设备写入」走硬件任务路径：APP 发任务、设备端放卡写入校验、状态回传；成功进 Collection，失败保留 Draft 可重发。已写入冲突换空白片时复用已就绪的云端 `contentId`／`nfc_url`，直接进入 NFC 写入，不重新上传或生成。

NFC 深链打开：声片写入 Well-Known URI 记录（TNF=0x01，type=`U`），按 NFC Forum URI RTD 使用前缀缩写码（`http://`→`0x03`，`https://`→`0x04`），payload 仅存 host+path；勿用 `0x00` 整段写入（NFC Tools 会显示 `Record 1` 而非 `Record 1 - http://`，系统读卡常打不开）。URI 放在 NDEF 消息首条；容量允许时再附 MIME／Text 绑定。目标链接形如 `http://{host}/c/{contentId}`（或 `https://`／`soundpola://c/{contentId}`）。**网页 `/c/{id}` 标题取云端 `display_label`**（须 App 已 PATCH 用户名称）；`<video src=/preview/{id}/video>` 依赖已上传本机 H.264。冷／热启动均进入 `/c/:contentId` 内容解析页（本机已有则进分类播放；未登录先登录再恢复；可 claim 云端记忆）。`go_router` 忽略平台原始 URI 作为初始 location，由 DeepLinkService 归一化后导航，避免 `soundpola://…` 被当成未知路由。

Drafts 保持本地优先且按账户隔离；Collection 登录后同步当前账号 READY 内容；列表按分类胶囊双列瀑布流；点击跑道进入 `/collection/category/:categoryId` 分类声片播放页（贴图环形进度 + 横向堆叠 + 完整记忆／声片／NFT）；取消独立 Memory 路由；长按声卡可拖入其他分类调整展示分类。

稀有度分级全息箔片（`RarityHoloStyle` / `RarityHoloOverlay`）：虹彩底膜＋衍射光栅＋RGB 色散高光＋微闪＋边缘焦散；N 银灰冷箔、R 薄荷青箔、SR 青蓝紫棱镜、SSR 全光谱＋密集微闪／强焦散／独占微粒视差；已接入 Collection 胶囊、分类堆叠、链上 NFT 卡与鉴定揭晓；分享卡中部声音可视化不加镭射。

统一空状态：`EmptyStatePanel` 覆盖 Record 权限阻断／过短、Drafts 三类空态、Collection 空／待封存／同步中／未登录、分类空、Press 无声音等；同步失败与空收藏分离。

真实麦克风录音、本地播放、NFC 读写（设备支持时）；云媒体契约见 `docs/openapi-cloud-media.yaml`。

Account 首页：昵称、收藏/声片数量、数字资产账户缩写、退出登录（Drafts／Collection 按账号隔离；换号清空视图）。**Lab** 底栏入口：点选音源 → 程序随机节拍阿卡贝拉（HotClip + 声部编排）；AI 网关暂关。

Drafts 本地优先且按登录账户隔离；Collection 登录后仅同步当前账号 READY 内容（不同账号互不串数据）。

待后续：完整首次引导、Account 子树完整页、真实链上 Mint（本 OpenAPI 不含 NFT）。地点 GPS 已在录音结果页写入并在播放页只读展示。

11. UI/UX 验收要点

未登录用户可以在不受打扰的情况下完成录音并保存 Draft。

任何 NFT 创建入口都会先确认账号归属。

登录完成后返回原 Press 任务，而不是回到首页。

Account 可清楚回答“我是谁、我的资产在哪里、绑定了哪些声片、如何恢复账号”。

Collection 与“我的数字资产”职责不同：前者面向记忆浏览，后者面向所有权凭证管理。

NFC 成功、上链失败、离线、权限拒绝和账号切换均有独立状态。

页面不要求用户理解 Gas、Mint、私钥或助记词才能完成核心流程。