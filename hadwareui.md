用户面对设备时，只需要理解两种使用方式：

手机发送声音，然后投入空白声卡
直接投入已绑定声卡，唤醒声音记忆

设备自动判断当前任务，不让用户选择“写入模式”或“播放模式”。

二、UIUX核心原则
1. 一屏只表达一件事

每个页面只回答三个问题：

设备现在在做什么；
用户是否需要行动；
声卡最终会从哪里出来。

不同时展示大量声音信息、系统参数和复杂进度。

2. 以“状态场景”代替“功能页面”

硬件端不设置：

首页导航；
播放器页面；
Collection；
声音列表；
设置中心；
模式选择；
声音视觉缩略图。

整个系统由事件自动推动：

接收任务 → 等待投卡 → 检测 → 写入或显影 → 自动出卡 → 待机

3. 减少触控操作

主要交互依靠投卡行为触发。触控屏只保留：

取消尚未开始的写入任务；
查看设备状态；
异常重试；
呼叫帮助。

一旦声卡进入内部机械流程，隐藏取消操作，避免用户误以为可以中断。

三、全新界面结构

屏幕采用固定的三层结构，而不是卡片堆叠。

1. 顶部：设备状态轨

高度约占屏幕的 10%。

显示：

SOUNDPOLA / MEMORY TERMINAL
手机连接
网络
NFC
全息装置
当前时间

使用小字号、细线与工业状态灯，不抢夺主信息。

2. 中央：任务舞台

占屏幕约 70%，只显示：

当前状态编号；
大字号核心文案；
极简过程图形；
当前声音名称或声卡编号；
必要的操作提示。

这里不出现声音波形、频谱、粒子、万花筒或任何声音生成视觉。

3. 底部：实体动作引导轨

占屏幕约 20%，用于连接屏幕与真实机械结构：

投卡方向；
出卡方向；
当前机械状态；
是否可以取卡；
简化的阶段进度。

底部色块可以像“机器内部托盘”一样移动，表达声卡正在进入、处理中或送出。

四、核心状态重新设计
01｜待机：等待一段声音

主文案：

投入声卡
唤醒一段声音

次级提示：

或在手机端选择声音，制作一张新声卡

屏幕表现：

大面积黑色留白；
中央只有投卡口方向的缓慢脉冲；
底部青色“感应轨道”处于低亮度；
顶部显示设备在线状态；
全息装置保持独立的微弱呼吸。

屏幕不展示作品封面或声音视觉。

02｜手机声音接收

主文案：

正在接收声音

显示：

声音名称；
文件大小或剩余接收时间；
线性数据传输指示；
手机连接状态。

不要提前提示投卡。

接收完成后，用短暂状态切换：

声音已准备好

随后进入等待投卡。

03｜等待写入

主文案：

请投入一张空白声卡

次级信息：

声音名称；
时长；
“一张声卡只能永久保存一段声音”；
等待任务剩余时间。

视觉表达：

底部青色托盘结构向投卡口方向延伸；
使用卡片轮廓或方向箭头表达“等待载体”；
不展示声音图像；
全息装置只显示未激活的低亮度状态。

可操作：

取消制作；
查看连接状态。
04｜声卡识别

主文案：

正在检查声卡

屏幕表现：

一个抽象的声卡轮廓；
NFC 扫描线由上至下经过轮廓；
显示 READING NFC；
底部机械轨道进入锁定状态。

提示：

声卡将在处理完成后自动送出

此时不显示按钮。

05｜自动写入

不使用常规百分比圆环，而采用“工序轨道”。

读取声卡 → 写入身份 → 校验绑定 → 创建凭证 → 准备出卡

当前工序高亮，已完成工序使用主题青色，未完成工序使用深灰色。

中央主文案随阶段改变：

正在写入声音身份
正在校验永久绑定
正在创建数字凭证
正在同步声音收藏
正在准备送出声卡

辅助信息：

声音名称；
声卡编号；
网络状态；
请勿关闭设备。

全息装置在此过程中逐渐生成声音视觉，但屏幕不复制该画面。

06｜显影播放

投入已绑定声卡后，不进入播放器页面，依次显示：

读取阶段

正在读取这张声卡

唤醒阶段

正在唤醒声音记忆

声音名称、时间和地点逐渐显现。

显影阶段

声音记忆正在显影

屏幕信息主动弱化，只保留：

声音名称；
录制时间；
录制地点；
分类；
非交互式时间状态；
HOLOGRAM ACTIVE。

不显示：

播放按钮；
暂停按钮；
可拖动进度条；
音量控制；
循环；
上一段或下一段；
声音生成视觉。

播放进程可以用屏幕底部非常细的“记忆刻度”自然推进，但不能做成播放器进度条。

07｜完成与自动出卡

写入完成：

声音已经保存下来

播放完成：

这段声音已经显影完毕

随后统一进入：

正在送出声卡

屏幕底部的声卡轮廓向出卡口方向移动，并与真实出卡位置对应。

检测到声卡掉出后：

请从出卡口取走声卡

取走后自动回到待机页面。

08｜异常状态

异常页面不使用弹窗，而是直接取代中央任务舞台。

页面结构固定为：

发生了什么；
声卡是否已经绑定；
任务是否被保留；
设备正在如何处理声卡；
用户下一步应该做什么。

例如：

已绑定声卡投入写入任务

这张声卡已经保存了另一段声音

原有声音不会被覆盖；
手机发送的任务仍然保留；
设备正在自动退回声卡；
请投入一张空白声卡。
写入完成但网络中断

声卡已经完成绑定

声音不会丢失；
数字凭证将在网络恢复后继续创建；
声卡正在安全送出；
无需再次写入。
机械卡滞

声卡未能正常送出

请勿再次投入声卡；
当前任务已经保存；
保持设备开启；
点击“获取帮助”。
五、重新构思视觉风格

不再使用“手机 App 大卡片＋多个胶囊按钮”的结构，而采用：

黑色仪器面板 × 高亮机械轨道 × 超大状态文字 × 精密工业标记

色彩层级
用途	色彩
主背景	#070808
次级面板	#111413
主题青色	#63E0CB
高亮青色	#8DFFE9
主文字	#F4F7F6
次级文字	#8C9693
未激活结构	#252B29
等待与警告	#FFAA54
严重错误	#FF626C

蓝、紫、粉渐变只属于全息装置。硬件屏幕应以黑、白、青为主，避免与全息影像争夺注意力。

字体层级
状态编号：等宽字体、小字号、全大写；
核心动作：超大无衬线粗体；
声音名称：中等粗体；
时间、地点、编号：等宽或窄体字体；
操作说明：常规无衬线字体。

示例：

MEMORY PROCESS / 03

正在写入
声音身份

雨落在帐篷上的夜晚
CARD  SP-07A38

请保持设备开启
图形元素

允许使用：

NFC扫描线；
声卡抽象轮廓；
数据刻度；
阶段轨道；
投入和送出方向；
模块编号；
低亮度网格；
小型状态灯；
类似设备铭牌的文字标记。

禁止使用：

声波；
频谱；
粒子图；
万花筒；
封面图片；
播放器图标；
多层内容卡片；
大面积彩色渐变。
六、屏幕与全息装置的关系
硬件屏幕	全息装置
解释设备正在做什么	呈现声音是什么样子
提供步骤和安全反馈	提供情绪与沉浸体验
使用黑白青工业语言	使用青、蓝、紫、粉动态视觉
状态清晰、运动克制	视觉流动、随声音变化
不显示声音生成视觉	不显示复杂文字和操作按钮

最终体验应形成清晰分工：

屏幕是设备的语言，全息影像是声音的形态，声卡是连接二者的实体媒介。