# SoundPola Memory Terminal · 硬件端 HMI PRD

文档用途：硬件 LCD（480×320）界面与状态机规格  
视觉预览：`docs/hardware-ui-preview/index.html`（权威画面）  
与 App 关系：手机发送声音／配对设备；硬件自动判断写入或显影，不设模式选择  
版本：对齐当前预览实现（已移除机械卡滞页）

---

## 一、产品定位

用户面对设备时，只理解两种使用方式：

1. **手机发送声音 → 投入空白声卡**（写入／封存）
2. **直接投入已绑定声卡**（唤醒／显影声音记忆）

设备根据投卡结果与当前任务自动分支，**不让用户选择「写入模式」或「播放模式」**。

核心链路：

```
接收任务 → 等待投卡 → 检测 → 写入或显影 → 自动出卡 → 待机
```

或无手机任务时：

```
待机 → 投已绑定卡 → 读取／唤醒／显影 → 自动出卡 → 待机
```

---

## 二、UI／UX 原则

### 1. 一屏只表达一件事

每屏只回答：

- 设备现在在做什么；
- 用户是否需要行动；
- 声卡最终会从哪里出来。

不同时堆叠声音详情、系统参数与复杂进度。

### 2. 以「状态场景」代替「功能页面」

硬件端**不设置**：

- 首页导航、播放器、Collection、声音列表、设置中心、模式选择；
- 声音视觉缩略图／波形／频谱／球体可视化（视觉只在全息装置）。

### 3. 减少触控

主要交互靠**投卡**触发。触控仅保留：

| 允许 | 禁止时机 |
|------|----------|
| 取消尚未开始的写入任务（接收中／等待投卡） | 声卡进入内部机械流程后 |
| 查看设备状态／连接状态 | — |
| 异常后续引导（文案驱动，少按钮） | — |

一旦轨道锁定（识别／写入／显影中），**隐藏取消**，避免误以为可中断。

---

## 三、屏幕规格与三层结构

| 项 | 规格 |
|----|------|
| 分辨率 | **480×320** |
| 产品名 | SOUNDPOLA MEMORY TERMINAL |
| 字体 | IBM Plex Sans（正文）／IBM Plex Mono（编号、铭牌、状态码） |
| 预览 | `docs/hardware-ui-preview/index.html` |

固定三层（非 App 卡片堆叠）：

### 1. 顶部 · 设备状态轨（约 10%／32px）

- 左：`SOUNDPOLA` + `MEMORY TERMINAL`
- 右模块灯：`PHONE` · `NET` · `NFC` · `HOLO` + 当前时间
- 灯态：熄灭／青色点亮／脉冲／警告橙／错误红
- 小字号、细线，不抢中央主信息

### 2. 中央 · 任务舞台（约 70%）

只显示：

- 状态编号（等宽，如 `STANDBY / 01`）；
- 大字号核心文案；
- 极简过程图形（脉冲环／声卡轮廓／NFC 扫描线／工序轨道／记忆刻度）；
- 声音名称或声卡编号；
- 必要操作提示与至多 1–2 个 ghost 按钮。

**禁止**：声波、频谱、粒子、万花筒、球体可视化、封面图、播放器控件。

### 3. 底部 · 实体动作引导轨（约 20%／64px）

连接屏幕与真实机械：

- 文案行：投／出方向、当前机械状态、是否可取卡；
- 托盘：`IN` · `NFC` · `OUT` 标记 + 可位移的声卡色块（表达进入／锁定／送出位置）。

---

## 四、色彩与字体

### 色彩

| 用途 | 色值 |
|------|------|
| 主背景 | `#070808` |
| 次级面板 | `#111413` |
| 主题青 | `#63E0CB` |
| 高亮青 | `#8DFFE9` |
| 主文字 | `#F4F7F6` |
| 次级文字 | `#8C9693` |
| 未激活 | `#252B29` |
| 警告 | `#FFAA54` |
| 严重错误 | `#FF626C` |

蓝／紫／粉渐变**只属于全息装置**；LCD 以黑、白、青为主。

### 字体层级

- 状态码：等宽、小字、全大写（`MEMORY PROCESS / 03`）
- 核心动作：超大无衬线粗体
- 声音名称：中等粗体
- 时间／地点／编号：等宽或窄体
- 操作说明：常规无衬线

### 允许／禁止图形

**允许**：NFC 扫描线、声卡抽象轮廓、数据刻度、工序轨道、投送方向、模块编号、低亮度网格、小型状态灯、铭牌文字。

**禁止**：声波、频谱、粒子、万花筒、封面、播放器图标、多层内容卡片、大面积彩色渐变。

---

## 五、状态机一览（与预览一一对应）

| ID | 状态码 | 场景 | 触控 |
|----|--------|------|------|
| 01 | `STANDBY / 01` | 待机 | 设备状态 |
| 02 | `RECEIVE / 02` | 手机声音接收 | 取消任务 |
| 02b | `READY / 02` | 声音已准备好（短暂） | 无 |
| 03 | `MEMORY PROCESS / 03` | 等待投入空白声卡 | 取消制作、连接状态 |
| 04 | `CARD CHECK / 04` | 声卡识别 | 无（轨道锁定） |
| 05 | `MEMORY PROCESS / 05` | 写入声音身份 | 无 |
| 05b | `MEMORY PROCESS / 05` | 校验永久绑定 | 无 |
| 06a | `REVEAL / 06` | 显影 · 读取 | 无 |
| 06b | `REVEAL / 06` | 显影 · 唤醒 | 无 |
| 06c | `REVEAL / 06` | 显影 · 播放中 | 无 |
| 07a | `COMPLETE / 07` | 写入完成 | 无 |
| 07b | `COMPLETE / 07` | 显影完毕 | 无 |
| 07c | `OUTPUT / 07` | 正在送出声卡 | 无 |
| 07d | `TAKE CARD / 07` | 请取走声卡 | 无 |
| 08a | `EXCEPTION / 08` | 已绑定卡投入写入任务 | 无（自动退卡） |
| 08b | `EXCEPTION / 08` | 写入完成但网络中断 | 无（安全送出） |

**已取消**：机械卡滞／「声卡未能正常送出」／「获取帮助」页（原 08c）。卡滞若发生，由设备固件与运维通道处理，**不进用户态 HMI 主流程**。

---

## 六、各状态文案与表现（权威）

### 01｜待机

- **主文案**：投入声卡／唤醒一段声音  
- **次文案**：或在手机端选择声音，制作一张新声卡  
- **图形**：中央投卡口方向缓慢脉冲环  
- **底部**：感应轨道 · 低亮度；托盘槽在 IN 侧弱亮  
- **按钮**：设备状态  
- **HOLO**：独立微弱呼吸（灯脉冲）

### 02｜手机声音接收

- **主文案**：正在接收声音  
- **元数据**：声音名称；体积 · 剩余约 mm:ss  
- **图形**：线性数据传输条（非圆环）  
- **底部**：请勿投卡 · 数据传输中  
- **按钮**：取消任务  
- **注意**：不要提前提示投卡；PHONE 灯脉冲

### 02b｜声音已准备好（短暂过渡）

- **主文案**：声音已准备好（青色）  
- **次文案**：即将进入等待投卡  
- **底部**：校验完成 → 03；托盘槽提亮  

### 03｜等待写入

- **主文案**：请投入一张空白声卡  
- **元数据**：声音名称；时长；「一张声卡只能永久保存一段声音」；等待剩余 mm:ss  
- **底部**：→ 投卡口 · 等待载体；托盘槽向 IN 延伸动画  
- **按钮**：取消制作、连接状态  
- **NFC**：脉冲  

### 04｜声卡识别

- **主文案**：正在检查声卡  
- **铭牌**：`READING NFC`  
- **图形**：声卡轮廓 + 自上而下扫描线  
- **提示**：声卡将在处理完成后自动送出  
- **底部**：机械轨道已锁定 · 无按钮；托盘槽锁定态  

### 05｜自动写入（工序轨道）

不用百分比圆环，用五段工序轨：

`读取 → 写入 → 校验 → 凭证 → 出卡`

| 子态 | 主文案 | 轨道高亮 |
|------|--------|----------|
| 05 | 正在写入声音身份 | 写入 |
| 05b | 正在校验永久绑定 | 校验 |

（后续工序文案可继续：正在创建数字凭证／正在同步声音收藏／正在准备送出声卡——预览以 05／05b 为代表。）

- **辅助**：声音名称；`CARD SP-×××××`；网络状态 · 请保持设备开启  
- **底部**：工序轨道 · 写入／校验中 · 请勿关闭；托盘锁定  
- **HOLO**：过程中逐渐生成声音视觉；**LCD 不复制该画面**

### 06｜显影播放（已绑定卡，无手机任务）

依次：

| 子态 | 主文案 | 要点 |
|------|--------|------|
| 06a | 正在读取这张声卡 | 轮廓 + 扫描；PHONE 可熄 |
| 06b | 正在唤醒声音记忆 | 名称、时间、地点渐显 |
| 06c | 声音记忆正在显影 | 名称／时间／地点／分类；`HOLOGRAM ACTIVE`；底部细「记忆刻度」推进 |

**不显示**：播暂停、可拖进度条、音量、循环、上下首、声音生成视觉。  
屏幕信息在 06c **主动弱化**（weak），让全息成为主体。

### 07｜完成与自动出卡

| 子态 | 主文案 | 底部 |
|------|--------|------|
| 07a | 声音已经保存下来 | 准备出卡 · 写入完成 |
| 07b | 这段声音已经显影完毕 | 准备出卡 · 播放完成 |
| 07c | 正在送出声卡 | → 出卡口 · 托盘向 OUT 移动 |
| 07d | 请从出卡口取走声卡 | 出卡口有卡 · 可取走（警告色提示） |

取走后自动回 **01 待机**。

### 08｜异常（取代中央舞台，不用弹窗）

固定回答：发生了什么 · 声卡是否已绑定 · 任务是否保留 · 设备如何处理声卡 · 用户下一步。

#### 08a｜已绑定声卡投入写入任务

- **主文案**（警告色）：这张声卡已经保存了另一段声音  
- **列表**：原有声音不会被覆盖；手机任务仍保留；设备正在自动退回声卡；请投入一张空白声卡  
- **底部**：→ 自动退卡 · 任务已保留；托盘外送动画  
- **NFC**：警告灯  

#### 08b｜写入完成但网络中断

- **主文案**（青色）：声卡已经完成绑定  
- **列表**：声音不会丢失；数字凭证将在网络恢复后继续创建；声卡正在安全送出；无需再次写入  
- **底部**：→ 安全送出 · 凭证后台续传  
- **NET**：警告灯  

---

## 七、屏幕与全息装置分工

| 硬件 LCD | 全息装置 |
|----------|----------|
| 解释设备正在做什么 | 呈现声音长什么样 |
| 步骤与安全反馈 | 情绪与沉浸 |
| 黑白青工业语言 | 青／蓝／紫／粉动态视觉 |
| 状态清晰、运动克制 | 随声音流动变化 |
| 不显示声音生成视觉 | 不显示复杂文字与操作按钮 |

最终体验：**屏幕是设备的语言，全息是声音的形态，声卡是连接二者的实体媒介。**

---

## 八、与 App 的边界

| 能力 | 归属 |
|------|------|
| 录音、Drafts、Collection、稀有度揭晓 UI | App |
| 扫码配对（`SNDPOLA1|ip|port|nonce|devid`）→ `POST /pair` | App ↔ 设备 |
| 发送待写入声音到已配对设备 | App → 设备（触发 02→03） |
| NFC 物理读写、入卡／出卡机械、全息渲染 | 硬件 |
| 手机 NFC 近场写入（无硬件时） | App 独立路径 |

App 侧 Drafts「写入机」拟物 UI 是**手机上的操作隐喻**，与本 HMI **视觉语言不同**（冷白 ABS 拟物 vs 黑色仪器面板）；产品逻辑上对应「把声音交给终端封存」。

---

## 九、验收要点

- [ ] 分辨率严格 480×320；三层结构不被破坏  
- [ ] 状态码／主文案／底部托盘与 `docs/hardware-ui-preview/index.html` 一致  
- [ ] 写入与显影无模式选择；由任务 + 卡状态自动分支  
- [ ] 轨道锁定后无取消按钮  
- [ ] LCD 无声音可视化；全息与屏幕分工清晰  
- [ ] 异常仅 08a／08b；无机械卡滞用户页  
- [ ] 取卡完成后回到 01，不残留任务舞台信息  
