SoundPola App UI 设计风格与视觉规范

文档用途：Flutter App 高保真 UI、设计系统、组件规范与动效实现版本：V2.0核心主题色：#63E0CB主题模式：仅深色主题，全局黑色背景技术基线：Flutter + Material 3 能力层，自定义 SoundPola 视觉层参考方向：Pinterest「产品 advx / App UI 设计参考」

1. 设计定位

1.1 一句话设计定义

以深黑空间承托会呼吸的薄荷青声音形态，把录音、实体声片和数字收藏统一为一座安静而有生命力的声音记忆馆。

1.2 风格关键词

黑色沉浸空间

感性科技

声音收藏品

薄荷青微光

有机生成

线性流动

克制信息

实体与数字连接

1.3 风格边界

黑底不等于传统音乐播放器或电竞界面。应避免：

大面积蓝紫霓虹渐变。

规则频谱柱、唱片和均衡器语言。

过多外发光、描边和玻璃层叠。

复杂区块链纹理、币圈图标和金融仪表盘。

每个元素都使用主题色。

画面重点始终是“声音生成的独特形态”，功能信息退居次级。

1.4 品牌标识

App 图标与启动字标使用同一套品牌图形（非角色插画）：

- 字标：斜体粗白字「SOUND / POLA」双行叠排，顶部「SOUND」背后为薄荷青轨道形色块。
- 背景：纯黑 `#000000`；强调色贴近主题 `#63E0CB` 系薄荷青。
- 资源：`assets/branding/app_logo.png`（源图）、`app_wordmark.png`（裁切字标）、`app_icon_1024.png`（方图主源）；Android `mipmap-*/ic_launcher.png`、iOS `AppIcon.appiconset` 由其生成（`dart run tool/generate_app_icons.dart`）。
- 桌面图标为字标居中的黑底方图；小尺寸以高对比白字 + 青色块保证可辨，不塞入角色或细线装饰。

2. 色彩系统

2.1 基础色

Token

色值

用途

Background / Primary

#000000

全局主背景

Background / Elevated

#070A09

底部导航、固定栏

Surface / 1

#0D1110

卡片、底部导航、输入框

Surface / 2

#141A18

弹窗、选中卡片、悬浮层

Border / Default

#26302D

默认分隔与描边

Border / Subtle

#19201E

弱分隔

Text / Primary

#F4F7F6

主标题与关键信息

Text / Secondary

#A5AFAC

辅助信息

Text / Tertiary

#68736F

时间、地点、说明

Accent / Primary

#63E0CB

主操作、录音状态、选中态

Accent / Soft

#9AEBDD

高光与视觉核心

Accent / Dark

#2E7D70

深色状态、低亮轨迹

2.2 语义色

状态

色值

使用原则

Success

#63E0CB

写入与上链完成

Warning

#F2C879

永久绑定、权限提醒

Error

#FF6B72

录音、NFC 或上链失败

Info

#8AB8FF

中性系统信息

2.3 主题色比例

75%：纯黑与深灰背景。

15%：白色与灰色文字。

8%：声音视觉中的薄荷青层次。

2%：按钮、状态点与关键选中态。

#63E0CB 应像“声音能量”一样出现，不作为大面积页面底色。

所有页面根背景统一为 #000000。卡片、输入框和弹层通过 Surface 层级区分，不使用浅色页面或白底登录页。

2.4 声片稀有度镭射

稀有度以镭射色与附加效果即时识别，等级越高越绚烂：

| 等级 | 镭射光谱 | 其它差异 |
| --- | --- | --- |
| N | 银灰 → 薄荷，弱、慢 | 单扫光带，安静描边 |
| R | 薄荷青 → 轻冷蓝 | 更清晰扫光，薄荷描边 |
| SR | 青 → 蓝 → 紫 | 双扫光带、次描边、外缘棱镜环 |
| SSR | 青 → 蓝 → 紫 → 粉（微量金） | 多带棱镜、粉紫描边、强光晕；独占边缘微粒／视差／揭晓爆发 |

实现：`RarityHoloStyle` + `RarityHoloSheenPainter` / `RarityHoloOverlay`；贴图上方独立层，不改原始贴图与声片尺寸。

3. 声音视觉

3.1 形态

四象限严格镜像线性声场（参考 visuallization Axis Field 密度与流动，对称改为四象限）：只在第一象限用 visualSeed 确定性随机生成多层纯圆形粒子线性排列（径向／弧向／弦向平行珠串），录成单次 Picture 后以 Canvas `scale(±1,±1)` 镜像出其余三象限——不重复计算、不四象限各画一遍。越出 Q1 的粒子直接跳过（禁止把 x／y 独立 clamp 到轴上，以免镜像后出现拉长条纹）。待机／安静样式沿线条更疏、珠径略大，保证读成独立圆形珠而非熔成香肠线；响亮样式再加密变细。内层密、外层疏；整体呈放射＋回环线束感，而非球体点阵或块状粒子云。

主体宽约屏宽 74%–82%。

禁止：伪 3D 球体、整图统一 scale、每帧随机重排线束、破坏四象限镜像。

3.2 色彩

背景 #000000；主色 #63E0CB；辅以亮深青 #2E9B88、青蓝 #6BC8F2、蓝紫 #A090E8、粉 #E8B8DC。光谱质心插值；onset 时外层高能珠短暂偏白。粒子亮度与透明度整体偏亮，安静态亦清晰可见。

3.3 动态节奏

空闲／完成／列表缩略：单帧静态（不跑 ticker）；录音／当前播放／暂停才连续动画。

录音：音量主驱动——小声运动慢、变化小；大声运动快、变化大。响度映射样式连续变形（尖锐度、密度、珠径性格、波纹结构、流速、色相）；频谱／onset 仅轻辅；粒子半径随样式性格变，整体半径几乎固定，禁止单纯放大缩小或随音量胀成一团。按设备帧率分档降粒子数与目标 fps（约 24–48）。

交互：拖动微调相位／折叠；双击复位；双指缩放 0.8–1.3（仅活动态）。

3.4 音频驱动（与视觉分离）

麦克风幅度流（主）→ Isolate：DC／RMS／自适应噪声底 → PixMusic 式快攻慢释峰值 AGC → soft gate（低底噪地板，保留动态）→ 多频段包络 → 128-bin 调制频谱 → flux／onset → `AudioFeatures`（分析约 30–50Hz；UI 通知约 25Hz）。CustomPainter 禁止直接处理原始 PCM／幅度。视觉以 `energy`（响度）为主：流速 ≈ 底速＋音量²×样式流速；形变幅度 `amp` 随音量从约 0.12→1.0；`volumeStyle` 由响度 softstep 映射到 A／B／C，且**滞后于 energy 缓变**（离开安静／待机样式时攻击更长），半段线性插值保证待机→中等→响亮形态连续可读。频谱局部只作亮度点缀。禁止硬切离散图案与整图 scale。

3.5 可视化留存（Indexed-MJPEG）

实时活动态按画质档目标 fps；录音中保存 AudioDrive 时间序列，不在实时帧率下截屏。录音／导入结束进入结果页即离屏按 visualSeed＋特征确定性重绘（低粒子档、逐帧 yield；`rendererVersion=soundpola_kaleido_linear_v3`），输出 12～15fps 独立 JPEG 帧流（`visual.mjpg`）＋字节偏移索引（`visual.idx`）＋`visual_manifest.json`＋`cover.jpg`（从同帧流选代表帧）；Android 再尽力编码同内容 H.264 `visual.mp4` 供云端 `POST .../video`。列表／卡面静态缩略用 cover；播放以音频时间为轴按索引取帧（解码去重＋小 LRU，未就绪用 cover／静态 canvas）——封面与播放画面必须来自同一次 bake。算法升级不改写旧帧流。

4. 字体与排版

Android 中文优先使用系统无衬线字体。

英文与数字可使用 Inter 或系统默认字体。

页面标题：28–32sp，Medium / Semibold。

卡片标题：16–18sp，Medium。

正文：14–16sp，Regular。

辅助信息：12–13sp，Regular。

录音计时可使用等宽数字。

主标题左对齐；录音计时与全屏状态可居中。

黑底上避免极细字重，正文对比度应满足无障碍要求。

5. 间距、圆角与层级

基础间距单位：4dp。

页面水平边距：20–24dp。

区块间距：24–32dp。

卡片内边距：16–20dp。

卡片圆角：20–24dp。

输入框圆角：14–16dp。

主按钮圆角：完整胶囊或 16–20dp。

弹窗圆角：24–28dp。

层级主要通过背景明度、留白和尺寸建立，不依赖强阴影。黑底卡片仅比背景亮一个层级，必要时使用 #26302D 的 1dp 描边。

6. 核心组件

6.1 主按钮

背景：#63E0CB。

文字或图标：#04110E。

高度：52–56dp。

按下：亮度降低并产生轻微缩放。

禁用：深灰背景与低对比文字，不仅降低透明度。

6.2 次按钮

深色表面背景。

#63E0CB 文字或图标。

1dp 深色边框。

不使用大面积发光。

6.3 声音卡片

深灰卡片承载声音视觉。

视觉缩略图占卡片面积的 60% 以上。

名称与播放是第一信息层。

时间、地点、分类是第二信息层。

NFT 和声片状态使用小型图标或标签，不抢占主视觉。

6.4 输入框

默认为 #0D1110 背景。

聚焦时使用 #63E0CB 细描边或底部光线。

标签常驻，避免只依赖占位文字。

描述输入框支持字数提示。

6.5 标签

未选中：深色表面与灰色文字。

选中：#63E0CB 的 12%–18% 透明底色、主题色文字。

分类标签不使用多个高饱和颜色，保证整体收藏感统一。

6.6 底部导航

四项：Record、Drafts、Collection、Lab，简洁线性图标（Lab 用 science 图标）。

悬浮磨砂玻璃胶囊，与屏幕左右边缘留白；背景模糊 + 低透明白。

选中项：#63E0CB。未选中：#969D9A。

点击轻微缩放与触觉反馈；无工业刻度或暗绿底板。

6.7 头像与 Account 入口

一级页右上角：磨砂玻璃圆形按钮。

6.8 顶部栏

背景与页面同为 #000000，滚动前不显示分割线。

页面标题与头像保持稳定位置。

详情页使用返回按钮，不同时出现底部导航。

滚动后可使用 #070A09 的轻微实色层，避免玻璃模糊影响性能。

6.9 弹层与确认框

普通选择使用底部弹层，背景 #141A18。

永久绑定、删除、注销使用居中确认框或独立确认页。

危险操作按钮使用 Error 色，主题色不用于危险确认。

Press 不可逆操作必须同时显示声音名称、视觉缩略图和声片编号。

7. 登录与 NFT 的视觉表达

7.1 登录页

延续黑底，不切换为传统白色表单页。

顶部左对齐大号品牌名「SoundPola」（约 32sp / Bold）。

从 Press 进入时，上半区保留当前待写入声音的缩略视觉，维持任务连续性。

表单区不使用外层卡片：账号 / 密码为独立胶囊输入框（圆角 30、填充 #171717），左侧 40% 白图标 + 占位文案。

「记住密码」为小方形勾选 + 正文。

主按钮实心薄荷青胶囊；次按钮透明底 + 深灰描边 + 主题色文字，圆角同为 30。

底部可有克制的薄荷青环境微光（大半径模糊椭圆），不作强霓虹。

主文案聚焦“保存与归属”，避免强调 Web3。

推荐文案：

登录后，这段声音将归属于你的数字收藏。

7.2 NFT

面向普通用户的主界面用词：

数字收藏。

数字资产编号。

链上凭证。

所有权状态。

技术详情折叠后再展示：

钱包地址。

区块链网络。

合约地址。

Token ID。

交易哈希。

NFT 的视觉标识使用薄荷青线框印章或小型凭证图标，不使用金币、六边形币标、紫色星云或交易图表。

7.3 Account

个人中心不是社交主页，应表现为私密的收藏账户：

头像与昵称低调展示。

收藏数量和声片数量优先。

最近收藏使用横向视觉卡片。

数字资产、设备、隐私与安全入口采用单层列表。

不展示无关的性别、生日、职业或社交关注数。

7.4 账号与 NFT 页面层级

页面

视觉重点

技术信息处理

登录

当前声音与账号归属

不显示钱包术语

Account

个人收藏身份与数量

只显示账户状态

我的数字资产

资产凭证与状态

Token ID 使用缩写

资产详情

声音视觉与所有权

链、合约、交易哈希折叠

账号与安全

登录方式与恢复能力

托管方式用解释性语言

权限与隐私

授权状态与数据范围

明确本地、云端、链上边界

8. 页面视觉重点

页面

第一视觉焦点

第二视觉焦点

弱化内容

Record

实时声音视觉

录音按钮与计时（主操作）

导入本地音频（次级文字入口）

权限和帮助

录音结果／导入结果

生成视觉

播放与保存

自动元数据

Drafts

黑色展示空间中的白色陶瓷精密写入机

页内插卡 + 磨砂玻璃 NFC 浮层

象牙白实体声卡 · 悬浮磨砂底栏

Press

当前阶段与声片关系

声音视觉

技术说明

Collection

分类胶囊叠片 + 双列瀑布流

玻璃拟物跑道 + 稳定声片贴图

分类声片播放页（环形进度 + 横向堆叠 + 完整记忆）

声片／NFT 信息折叠展示

Account

个人收藏身份

资产与设备入口

钱包技术信息

9. 动效与触觉

页面转场：220–320ms，使用柔和减速。

卡片进入：轻微淡入与 8–12dp 上移。

播放：视觉内部流动，不让整张卡片跳动。

NFC 检测：缓慢扩散的同心波或近场光晕。

写入成功：声音视觉向声片轮廓收束，再扩散成收藏图。

上链：使用连续轨迹连接节点，避免币圈式旋转加载图标。

主操作与 NFC 识别配合短促触觉。

永久绑定确认使用一次更明确但不过强的触觉反馈。

支持“减少动态效果”，保留状态变化而降低流动幅度。

10. 无障碍与可用性

文本与背景对比度至少达到 WCAG AA。

#63E0CB 不单独作为状态区分，同时配合图标和文字。

触控区域不小于 48 × 48dp。

录音、暂停、完成不可只靠颜色区分。

错误信息说明原因和下一步操作。

永久绑定、注销和删除采用明确的二次确认。

黑底界面在低亮度下避免大面积纯白闪屏。

11. UI 交付检查

全部页面统一为黑色背景体系。

#63E0CB 只用于能量、选中态和关键动作。

声音视觉具有线性流动感，不出现块状拼接感。

Record / Drafts / Collection / Lab 保持四项底部导航。

Account 通过右上角头像进入。

未登录用户可录音和使用本地 Drafts。

Press 前完成登录，并保持声音任务上下文。

Collection 与 Account 中不重复堆叠 NFT 信息。

钱包与链上技术字段默认折叠。

所有关键空状态、错误状态、权限状态与离线状态均有设计。

统一组件：`EmptyStatePanel`（`mobile/lib/widgets/empty_state_panel.dart`）。

变体：`firstUse` / `cleared` / `filtered` / `blocked` / `processing` / `error`。

五层结构：保留页标题导航 → 空设备视觉 → 英文状态码 → 中文标题与说明 → 单一主按钮（可选文字次按钮）。

空设备色：`#080C0B` / `#151B19` / `#252D2B`；主文 `#F2F5F4`；说明 `#727A78`；品牌青仅状态点与主按钮。亮度约正常内容的 35%–50%。轻呼吸与 1–2px 悬浮；系统「减少动态效果」时仅保留透明度变化。

同步中 / 网络离线 / 服务不可用等不得伪装成「没有数据」；Collection 仅在同步确认资产为 0 后才显示 `COLLECTION EMPTY`。

Drafts / Press / Collection 文案必须区分：Drafts 强调暂存待封存；Press 强调需要声音与实体声片；Collection 强调写入并上链后才成为资产。

12. Flutter 设计系统映射

12.1 Theme 基础

使用 ThemeData.dark(useMaterial3: true) 作为能力基础，但不直接沿用默认 Material 视觉。

scaffoldBackgroundColor、全屏弹层和导航容器根背景统一为 #000000。

colorScheme.primary 使用 #63E0CB。

colorScheme.surface 使用 #0D1110。

colorScheme.error 使用 #FF6B72。

颜色、字号、圆角、间距和动画时长全部通过 Token 管理，页面内不直接写色值。

系统状态栏与导航栏保持黑色；图标使用浅色模式。

12.2 推荐 Token 命名

SpColors.bgPrimary
SpColors.bgElevated
SpColors.surface1
SpColors.surface2
SpColors.accent
SpColors.textPrimary
SpColors.textSecondary
SpColors.error

SpSpacing.xs / sm / md / lg / xl
SpRadius.input / card / dialog / pill
SpMotion.fast / normal / slow

12.3 组件拆分

Flutter 组件

职责

SpScaffold

黑底、SafeArea、状态栏与页面边距

SpTopBar

标题、返回、头像和状态入口

SpBottomNav

Record / Drafts / Collection / Lab

SoundVisualCanvas

四象限镜像线性粒子线束（参考 visuallization；静态单帧／活动态分档）

RecordControl

录音、暂停、继续、完成

SoundCard / DraftTrayCard

Draft 横向错位暂存卡片（圆形贴图 + 环形进度）

PressMachine

拟物化声音写入机器（顶部插口 + 毛玻璃仓内多张贴图圆形声片重力掉落／两两碰撞防重合 + 像素状态屏）

CollectionCapsule

Collection 玻璃拟物分类跑道（瀑布流单元）

CategoryPlayStage

分类播放页：圆形声音可视化舞台 + 横向三维声片堆栈

PlaybackBar

播放、进度和时长

SpPrimaryButton

主任务操作

SpTextField

名称与描述输入

SpCategoryPicker

单选分类；列表为用户已创建名称，可新建

StatusChip

写入、上链、失败和绑定状态

PressProgress

NFC、校验、Mint、上链分阶段进度

AssetCredentialPanel

默认折叠的 NFT 技术信息

Draft 使用横向错位托盘卡片（DraftTrayCard）+ 拟物写入机（PressMachine）；Collection 使用分类胶囊叠片（CollectionCapsule），不再复用同一套网格卡片骨架。

12.4 响应式与设备适配

以手机竖屏为主要画布，设计基准宽度可采用 360–390dp。

使用 SafeArea 适配刘海、挖孔与底部手势区。

不依赖固定像素定位声音视觉；根据可用宽度和最短边计算尺寸。

小屏设备优先保证录音控制、播放和 Press 主按钮完整可见。

大屏或平板保持内容最大宽度，避免卡片无限拉伸。

键盘出现时保证名称、分类、描述和保存按钮可滚动到可视区域。

12.5 原生能力与性能边界

录音、NFC、蓝牙、定位与触觉由独立服务封装，UI 不直接持有平台调用。

实时视觉优先使用 CustomPainter、Fragment Shader 或性能可控的渲染方案。

动画目标为稳定 60fps；低性能设备允许降低分辨率与刷新率。

录音过程中避免大面积模糊、实时阴影和多层透明叠加。

页面退出时正确释放录音流、动画控制器与传感器监听。

13. 黑底界面专项验收

启动、权限、登录、录音、列表、详情、Account 与异常页均保持纯黑根背景。

页面切换、系统键盘、权限弹窗返回和加载过程不出现白色闪屏。

Surface 层级可辨认，但不会形成一块块过亮的灰色面板。

#63E0CB 只用于核心动作、录音能量和关键状态。

文字对比足够，辅助文本不因黑底而过暗。

OLED 屏幕下视觉中心清晰，同时避免长时间固定高亮元素造成干扰。